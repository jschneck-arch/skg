# SKG Review Ledger

Audit pass date: 2026-03-30

Status legend:

- `deep_inspected`: files were opened and read in detail for this pass
- `mixed`: key files were deeply inspected; remainder of subsystem was inventory-mapped only
- `mapped_only`: subsystem was inventoried and sampled, but not read end to end
- `non_canonical`: present in repo but not treated as canonical implementation

## Repository-Wide Ledger

| Subsystem | Status | Evidence opened this pass | Notes |
|---|---|---|---|
| Root docs and engagement framing | `deep_inspected` | `README.md`, `ENGAGEMENT.md` | Root intent and operator framing are current and useful. |
| `docs/` architecture and formal papers | `deep_inspected` | `SKG_ARCHITECTURE_SYNTHESIS_20260328.md`, `SKG_CANONICAL_RUNTIME_MAP.md`, `SKG_RUNTIME_UNIFICATION_PLAN.md`, `SKG_RUNTIME_ARCHITECTURE.md`, `SKG_CANONICAL_DATA_MODEL.md`, `SKG_FIELD_FUNCTIONAL.md`, `SKG_STATE_TRANSITION_MODEL.md`, `SKG_INFORMATION_ENERGY_AND_GRAVITY_MODEL.md`, `SKG_CLOSED_OBSERVATION_LOOP.md`, `SKG_Work3_Final.md`, `SKG_Work4_Final.md`, workflow/playbook/operator docs, model docs, audit docs | Core ideas are documented, but some docs are stale/skeletal relative to code. |
| Packaging / bootstrap / install | `deep_inspected` | `pyproject.toml`, `requirements.txt`, `bin/skg`, `install.sh`, `install_layer4.sh`, `setup_arch.sh`, `scripts/skg.service`, `scripts/skg-train.service`, `scripts/skg-train.timer`, `scripts/skg.env.template` | Packaging is modernized via `pyproject.toml`, but install/bootstrap scripts are partially stale; both `install.sh` and `setup_arch.sh` now contain the same statically broken bond smoke assertion, `install_layer4.sh` expects multiple classes that no longer exist, and the training service/timer pair is operationally inconsistent because the service is templated with `User=%i` while the timer targets the non-instantiated unit name. |
| Config / contracts | `deep_inspected` | `config/skg_config.yaml`, `targets.yaml`, `data_sources.yaml`, `daemon_domains.yaml`, `assistant_contracts.yaml`, `coupling.yaml`, `config/contracts/transactions.json` | Domain registry and contract config are important and active. |
| `skg/core/` | `mixed` | `daemon.py`, `paths.py`, `domain_registry.py`, `daemon_registry.py`, `coupling.py`, `assistant_contract.py`, `state_db.py` | Daemon and registry paths were deeply inspected; later passes traced `/collect`, `_find_projection_files()`, `_all_targets_index()`, `_identity_relations()`, `_identity_world()`, topology registration, assistant endpoints, and found a duplicate `/delta/workloads` route. The daemon `/collect` path now defaults `workload_id` to `ssh::{target}`, aligning the daemon-online shape with CLI fallback manifestation naming. `domain_registry.py` actively selects nested projectors before root projectors. `state_db.py` now looks less fully unwired than first recorded, but still only as a write-side shadow: current repo-wide callsites construct it in `gravity_field.py` and write credentials, pivot targets, and wicket snapshots, while no live read-side consumers were found for its query methods. This pass also confirmed `coupling.py` still numerically privileges older domain names in the default inter-local table, so current `data_pipeline` / `binary_analysis` / `container_escape` naming can fall back to near-default coupling unless config overrides it. A later daemon/topology alias pass confirmed `_find_projection_files()` only normalizes `binary <-> binary_analysis`, so daemon projection endpoints can still miss equivalent legacy `data` projections when queried as `data_pipeline`. A follow-on field-state pass also confirmed daemon-side `target_domains` derivation has no `binary_` branch at all, so live binary field-state rows receive no topology/fiber pull. The latest daemon-world pass confirmed `_identity_manifestations()` also zeroes binary and AD manifestation scores because it only reads host/web/data/container/AI score keys, the newest daemon-world recency pass confirmed `_identity_manifestations()` also keeps the first interp row it encounters for a `(workload_id, attack_path_id)` pair rather than the newest one, so manifestation rows can depend on directory iteration order, the latest daemon-world summary pass confirmed `_identity_world()` reports `world_summary["manifestation_count"]` from projection-row count rather than unique manifestation count and that `list_targets()` surfaces that same summary directly, and the newest target-list manifestation pass confirmed `list_targets()` can also return `manifestations=None` while the adjacent `world_summary.manifestation_count` is nonzero because the top-level manifestations field only comes from the measured-view index. The latest artifact-attribution pass confirmed `_artifact_matches_identity(...)` accepts filename token matches before checking payload identity, so `/artifacts/{identity_key}` and assistant artifact context can attribute unrelated files to an identity. The latest artifact-preview pass then confirmed the public `/artifact/preview` helper still returns full JSON payload content in its `.json` branch despite presenting itself as a bounded preview. The latest cache pass confirmed the first `/status` or assistant-context caller after cache invalidation can see `{}` field-state while recompute runs in the background, the latest timeline pass confirmed `identity_timeline()` only discovers workloads from current `interp/` artifacts rather than from broader manifestation/history state, the latest assistant-bundle pass confirmed graph-context seeding ignores `timeline["workloads"]` and can pivot onto neighbor workload ids instead, and the latest assistant-summary pass confirmed target summaries can report zero active paths when field state exists but measured-surface grouping is empty. The latest assistant-endpoint pass then confirmed `/assistant/explain` and `/assistant/what-if` largely propagate those same context defects unchanged because both route through `_assistant_prepare_context(...)` and return bundle-derived references directly, and the latest assistant metadata pass confirmed both `bundle.artifacts.count` and assistant `references` counters are preview-sized rather than total counts. |
| `skg/assistant/` | `deep_inspected` | `assistant/action_proposals.py`, `assistant/demands.py`, `assistant/writer.py`, `assistant/validators.py` | Assistant output classification and artifact/proposal wiring are among the cleaner, stricter boundaries in the repo. |
| `skg/cli/` | `mixed` | `cli.py`, `cli/app.py`, `cli/utils.py`, `cli/commands/target.py`, `cli/commands/report.py`, `cli/commands/gravity.py`, `cli/commands/proposals.py`, `cli/commands/exploit.py`, `cli/commands/data.py`, `cli/commands/surface.py`, `cli/commands/system.py`, `cli/commands/check.py`, `cli/commands/replay.py`, `cli/commands/intelligence.py`, `cli/commands/toolchains.py`, `cli/commands/core.py`, `cli/commands/derived.py`, command inventory | CLI architecture and main flows were inspected; proposal execution contains scenario-specific DVWA/CMDI logic, SSH collect depends on the broken `/collect` path, and direct web observe still writes discovery artifacts instead of the canonical measured event path while also omitting `--workload-id`, causing collector output to default to bare host identity strings. The parser exposes `engage` but no `train` command, `data` does not expose the `redteam` flow that `skg.intel.redteam_to_data` advertises, and `engage clean` still deletes orphaned projections by exact `workload_id` string equality. Later passes confirmed that `report` now emits an explicit daemon-offline warning for missing fold data, `proposals list` has a dead maturity-display branch, and `proposals trigger` hardcodes `host_linux_privesc_sudo_v1` for post-session host projection even though the host toolchain exposes multiple host attack paths. This pass further confirmed that `status` and `web` still rank raw discovery surfaces rather than a measured view, fold helpers still fall back to offline disk state when daemon fold rows/summary are empty, `cmd_surface` collapses projection display by attack-path id across all targets while also scanning loose `DISCOVERY_DIR` and `/tmp` interp artifacts, `cmd_replay` is only an approximate replay despite claiming live-equivalent substrate behavior, `cmd_check` validates WinRM availability via the wrong import name (`pywinrm` instead of `winrm`), the `field` parser hardcodes a stale domain enum that rejects active `binary`/`data_pipeline` values, and `derived rebuild` is not actually substrate-only even though the help text claims append-only reconstruction. The newest surface-subject pass confirmed `_surface_subject_rows()` groups measured `surface()` path rows by identity and dedupes manifestation keys, so CLI `surface`, `target list`, and `report` operate on a third aggregation shape distinct from both raw measured path rows and daemon world-summary row counts. |
| `skg/identity/` | `deep_inspected` | `identity/__init__.py` | Identity parsing is a real canonical layer. |
| `skg/substrate/` | `deep_inspected` | `node.py`, `path.py`, `projection.py`, `state.py`, `bond.py`, `__init__.py` | Substrate model is substantive and active. |
| `skg/kernel/` | `deep_inspected` | `observations.py`, `state.py`, `energy.py`, `projections.py`, `reason.py`, `contexts.py`, `gravity.py`, `identities.py`, `pearls.py`, `support.py`, `engine.py`, `field_local.py`, `field_functional.py`, `folds.py`, `adapters.py`, `__init__.py` | Formal model implementation is real, not ceremonial. This pass added two sharper kernel-layer drifts: `KernelStateEngine` still carries a stale fallback prefix map (`DA-`, `BI-`, `LA-`, `IO-` era naming), so `field_locals()` / fiber scoring can collapse active AD, binary, data, and IoT wickets into one `unknown` local unless callers provide explicit `domain_wickets`; and `adapters.py` still selects discovery artifacts by `node_key`-shaped filename patterns, so hostname identities can miss IP-named discovery files even when event payload identity matches. |
| `skg/graph/` | `deep_inspected` | `graph/__init__.py`, `kernel/wicket_graph.py`, `kernel/pearl_manifold.py`, `kernel/pearls.py` | Graph code is active and bounded: it propagates priors, not truth, but still auto-infers edges from event metadata. The newer wicket-graph and pearl-manifold layers are also real and runtime-used, but they lag some active domain naming: current catalog seeding misses the AI catalog filename and prefix-domain inference still treats `DP-*`, `DE-*`, and `IF-*` as `unknown`, which weakens domain expansion and dark-hypothesis reporting for data, IoT, and AI. |
| `skg/temporal/` | `deep_inspected` | `feedback.py`, `interp.py`, `__init__.py` | Temporal ingestion is active; filename heuristics remain an issue. A later full read of `temporal/__init__.py` found that `DeltaStore.calibrate_confidence_weights()` currently reasons over `evidence_rank` / metadata that `WicketTransition` does not record, so the calibration helper falls back to defaults even when real transitions exist. The follow-on `feedback.py` pass confirmed this layer is live in daemon/sensor flows and pearl-backed rather than ceremonial; its main current weakness is domain-inference fallback breadth rather than total integration. |
| `skg/intel/` | `deep_inspected` | `__init__.py`, `surface.py`, `gap_detector.py`, `engagement_dataset.py`, `redteam_to_data.py`, `confidence_calibrator.py` | Surface synthesis, engagement-dataset conversion, and cross-domain derivation are implemented, but `surface.py` no longer matches older `SurfaceBuilder`/doc expectations cleanly, `redteam_to_data.py` is not exposed by the actual CLI despite advertising `skg data redteam`, and `intel/__init__.py` still references the old `toolchain_forge.py` name instead of the live `skg.forge` package. A stronger correctness defect is now confirmed in `engagement_dataset.py`: the default build path uses `SKG_STATE_DIR` instead of `DELTA_DIR`, so transition ingest misses the canonical `delta/delta_store.ndjson` location unless `delta_dir` is passed explicitly. A later `surface.py` pass found another direct ranking bug: explicit `data_pipeline` and `binary_analysis` projections currently miss their score-key/domain-label mapping and can surface with score `0.0` even when the interp payload carried `data_score` or `binary_score`. |
| `skg/topology/` | `deep_inspected` | `energy.py`, `manifold.py`, `sheaf.py`, `kuramoto.py`, `__init__.py` | Topology is active but still fed by hybrid inputs. This pass also confirmed one naming/coupling seam below the main energy engine: `manifold.py` and `sheaf.py` are structurally coherent, but topology-facing helper layers still bridge old and current sphere/domain names inconsistently, and current `data_pipeline` locals can miss topology contribution when helpers expect `data`. A later daemon/topology alias pass confirmed `FIELD_DOMAIN_TO_SPHERE` still maps `binary_analysis` to `host` rather than `binary`, and `_world_states_from_surface()` still ignores current `data_pipeline` and `binary_analysis` target domains. |
| `skg/resonance/` | `deep_inspected` | `engine.py`, `ingester.py`, `drafter.py`, `llm_pool.py`, `memory.py`, `observation_memory.py`, `ollama_backend.py`, `cli.py`, `embedder.py` | Resonance is substantive, not placeholder code; some ingester docstrings lag the actual hard-coded source mapping. This pass confirmed a sharper workflow split: `draft_prompt()` / `draft_accept()` exist as file-based helpers, but the live CLI exposes only `draft` / `drafts`, the no-backend path in `draft_catalog()` raises instructions for non-existent `draft-prompt` / `draft-accept` subcommands, and `OllamaBackend` docs still mention `sensors.yaml` while config loading comes from `skg_config.yaml`. A deeper follow-on read found stronger contract drift: `draft_prompt()` and the API-key prompt builder expect tuple-style surface context while `ResonanceEngine.surface()` returns `{\"record\": ..., \"score\": ...}` dictionaries, the standalone `skg.resonance.cli` `drafts` path calls missing `engine.list_drafts()`, adapter discovery relies on hard-coded evidence-source maps plus regex scanning, `DomainMemory.adapters` is never populated even though the memory model advertises it, workload-scoped history still mixes exact-wicket records across different workloads, and the TF-IDF fallback embedder changes vector weights over time while the append-only indexes keep old vectors. |
| `skg/forge/` | `deep_inspected` | `pipeline.py`, `proposals.py`, `validator.py`, `generator.py`, `compiler.py`, `__init__.py` | Forge pipeline and persistent proposal lifecycle are real and wired, though generated outputs need deeper validation. A newly confirmed defect is that proposal accept/reject paths call the training corpus hooks twice, so accepted/rejected toolchain examples are likely duplicated in the learning corpus. |
| `skg/sensors/` | `deep_inspected` | `__init__.py`, `projector.py`, `ssh_sensor.py`, `web_sensor.py`, `data_sensor.py`, `cve_sensor.py`, `bloodhound_sensor.py`, `context.py`, `confidence_calibrator.py`, `struct_fetch.py`, `dark_hypothesis_sensor.py`, `agent_sensor.py`, `boot_probe.py`, `cognitive_sensor.py`, `gpu_probe.py`, `msf_sensor.py`, `net_sensor.py`, `process_probe.py`, `usb_sensor.py`, `zero_day_detector.py` | Core loop and sensors are now line-by-line inspected across the full canonical sensor tree. Run-scoped auto-projection still falls back to recent full scans because sweep filtering keys on the loop `run_id` while active sensors emit files with independent run-id suffixes (or no run-id suffix at all). The live web sensor remains on the incompatible `WEB-*` namespace, and it emits those ids on the same `skg-web-toolchain` lane used by the checked-in `WB-*` projector. This pass confirmed additional active defects in the newly inspected sensor subset: `DataSensor` dereferences undefined `_events_dir` / `_graph` / `_cfg` attributes on live paths, `zero_day_detector` resolves `SKG_HOME` to `/opt` and checks version coverage against wicket-id sets, `process_probe` emits literal template expressions in detail strings for PR-03/PR-09/PR-10, and `MsfSensor` persists `seen_sessions` but does not suppress repeated session emission. |
| `skg/gravity/` | `deep_inspected` | `runtime.py`, `selection.py`, `landscape.py`, `failures.py`, `__init__.py` | Decomposed gravity library is active and tested. It is also more measured-view aware than the monolithic shell: domain derivation, tool-overlay aggregation, and instrument ranking explicitly consume measured view state. This pass found one important alias lag inside that cleaner layer: `selection.py` still infers instrument spheres from stale `DA-` / `BI-` / `CE-` style prefixes, so domain-hint boosts and sphere-weighted selection can miss current binary, data, and IoT wavelengths unless instrument hints happen to compensate. |
| `skg-gravity/` | `mixed` | `gravity.py`, major sections of `gravity_field.py`, `exploit_dispatch.py`, `cred_reuse.py`, `gravity_web.py`, `exploit_proposals.py`, `adapters/__init__.py`, `adapters/impacket_post.py`, `adapters/ldap_enum.py`, `adapters/openssl_tls.py`, `adapters/smbclient.py`, `adapters/theharvester.py`, adapter inventory | Major control paths were inspected; `gravity_field.py` is now deeply covered across identity/fold handling, instrument discovery, dispatch, execution, and the main cycle, though it is still too large to claim one-pass total review. The web-side gravity logic clearly expects a broader `WB-*` vocabulary than the checked-in web catalog defines, schedules `web_struct_fetch` for `WB-30..WB-40`, and generates follow-on exploit proposals for path IDs absent from the checked-in catalog. The smaller sidecar modules are weaker: `gravity.py` is only a compatibility shim, repo-wide search found no live callsites for `gravity_web.py` or `exploit_proposals.py`, and `exploit_proposals.py` writes to its own `state/exploit_proposals` queue instead of the shared proposal lifecycle. A deeper gravity-field read later found several stronger active-path defects: `_exec_ai_probe()` rewrites adapter-authored AI event files into summary dicts before projection; `_exec_post_exploitation()` can raise `UnboundLocalError` before session discovery when target OS metadata is absent; multiple gravity-generated host/web events still reuse canonical wickets with conflicting meanings; several plugin adapters (`ldap_enum`, `openssl_tls`, `smbclient`) emit `toolchain="skg-gravity"` events that the normal projector will not interpret and currently omit required `pointer` fields in their `envelope(...)` calls; the main gravity cycle currently drops subject `identity_key` on execution while also comparing pre-run and post-run energy with different term sets; `_exec_ssh_sensor()` appends returned host events back into the same file that `run_ssh_host()` already wrote; `_exec_sysaudit()` emits raw event files without projecting them; a dead duplicate BloodHound collector body is still embedded inside `_exec_data_profiler()` after its `return`; `_merge_configured_targets()` still assumes dict-shaped `targets.yaml` and can crash on list-root configs; and `execute_instrument()` still contains duplicated dead `iot_firmware` / `ai_probe` / `supply_chain` dispatch branches. |
| `skg/sensors/bloodhound_sensor.py` and BH support path | `deep_inspected` | `sensors/bloodhound_sensor.py`, `sensors/adapter_runner.py` | The native BH sensor is substantive and routes through the existing AD adapter, but the implementation drifts from its advertised model: `domain_sid` is accepted but not used to scope collection, default workload identity falls back to the BloodHound endpoint hostname, and gravity-side availability currently depends on CE URL reachability even when Neo4j fallback is configured. |
| Native toolchains | `mixed` | APRS, container escape, AD lateral, host, data: primary projection, primary adapter, catalogs, golden-test presence | Deep enough to understand structure and naming; not every adapter/test file opened. This pass added line-by-line inspection of `skg-host-toolchain/adapters/msf_session/parse.py`, which emits raw host workload ids (or `msf_workspace`) rather than the `host::...` manifestation shape used by the CLI trigger shell. A later data-toolchain pass found a sharper contract split: `db_discovery` is a real operator-facing path via `skg data discover`, but its DE-* events omit both `provenance` and `attack_path_id`, so they do not match the normal measured event envelope used elsewhere. Follow-on reads found that the APRS root CLI and projector still default to `contracts/catalogs/attack_preconditions_catalog.v1.json`, which is coherent inside APRS but older than the repo-wide `{domain}.v1` catalog naming convention. Narrow golden validation now shows APRS, container escape, AD lateral, and host are at least internally coherent on their checked-in fixtures, though the coverage shape differs materially: APRS golden is a three-event projector-only check, AD lateral golden is effectively BloodHound-only, container escape golden exercises both the adapter fixture and projector with a broader CE-* set, and host golden remains projector-only. A deeper host pass also found one concrete hygiene defect: `skg-host-toolchain/adapters/ssh_collect/parse.py` contains a second unreachable copy of its main collection flow after an early `return 0`. The same host pass found a lighter catalog drift: host wicket `evidence_hint` minima for `HO-01`, `HO-02`, and `HO-04` still describe weaker network-grade evidence, while live SSH/WinRM adapters now emit stronger rank-1 confirmations for those same conditions. The newest data-adapter pass confirmed `db_profiler.profile_table(...)` accepts `attack_path_id` but drops it from emitted payloads, so per-source data path ids from sensor/config inputs are currently not preserved into measured events. |
| Auxiliary / generated toolchains | `mixed` | web, nginx, binary, AI, IoT firmware, supply chain, metacognition: primary projection, primary adapter or probe, catalogs, `forge_meta.json` where present | Several appear generated/template-derived and need focused validation later; the web toolchain currently spans incompatible collector/catalog/projector/gravity vocabularies, includes semantic `WB-*` collisions between active collector and checked-in catalog, and still contains a stale root projector. This pass confirmed that root projector is more broken than first recorded: it points at a non-existent catalog and ignores `workload_id` in `latest()`. The IoT firmware probe also has a concrete false-negative path for HTTP-only devices because `probe_device()` exits before any HTTP request when no spontaneous TCP banner is seen, while a `probe_network_only()` fallback exists but is currently unwired. A deeper follow-on read found a stronger internal split: `skg-iot_firmware-toolchain/adapters/firmware_probe/__init__.py` and `probe.py` are two different adapter implementations with different collection modes and default attack-path ids. The same pass also found fragile optional sheaf-import shims in nested projectors such as IoT firmware, data, host, AD lateral, container escape, and supply chain: `Path(__file__).resolve().parents[4]` resolves to `/opt`, not `/opt/skg`, so sheaf refinement silently drops out unless imports happen to work for unrelated reasons. Inventory and root-file reads also show that several of these toolchains are especially thin at the repo root: AI, nginx, metacognition, and web rely on catalogs/projectors plus `forge_meta.json` with no local tests, while `skg-data-toolchain/tests/` currently contains only an empty `__init__.py`. A deeper supply-chain read found a stronger contract problem: the active `sbom_check` adapter maps `SC-10` to `requests`/`urllib3` CVEs rather than the checked-in catalog’s `cryptography_high_vuln_present` semantics, and the checked-in supply-chain catalog still contains `SC-11`/`SC-12` wickets that no attack path requires and the adapter never emits. Follow-on reads of metacognition and web helper code found two more concrete mismatches: metacognition adapters emit `obs.substrate.node` records that do not match the normal SKG event envelope shape, and the metacognition projector is history-sticky because it lets any prior `blocked` state outrank newer `realized` evidence. On the web side, helper adapters such as `auth_scanner`, `gobuster_adapter`, `nikto_adapter`, and `sqlmap_adapter` continue the `WB-*` semantic drift and, in several cases, emit partial/manual event records with URL-shaped workload ids instead of the canonical helper envelope. A later pass found one more stale path inside the canonical web tree itself: `skg-web-toolchain/adapters/ssh_collect/parse.py` still emits legacy `W-01..W-05` Apache/APR wickets and appears only in itself, `forge_staging`, and older evidence/doc artifacts rather than in the active runtime/test surface. By contrast, the AI probe and auxiliary binary adapters are comparatively contract-aligned: `ai_probe`, `capa_analysis`, `angr_symbolic`, and `frida_trace` all use the normal event envelope and programmatic `run()` entry points, but they still have little or no direct behavioral coverage. |
| Tests | `mixed` | `test_runtime_regressions.py`, `test_gravity_runtime.py`, `test_gravity_routing.py`, `test_sensor_projection_loop.py`, `test_cli_commands.py`, `test_dark_hypothesis_sensor.py`, `test_ollama_backend.py`, `test_resonance_drafter.py`, `conftest.py`, suite inventory, `pytest --collect-only -q` | Test suite is real and broad; not every test file was read in detail. `test_runtime_regressions.py` provides meaningful identity/custody/projection-registry coverage, including binary aliasing and dynamic sensor discovery, but current focused coverage is still stronger on event/contract correctness than on CLI/daemon/web integration boundaries. `test_dark_hypothesis_sensor.py` only exercises prompt/JSON planning behavior and patches away real instrument discovery plus proposal-lifecycle integration. `test_ollama_backend.py` only covers config loading, `test_resonance_drafter.py` only covers the direct Ollama happy path with backend methods patched, and `test_gravity_routing.py` is titled as an integration routing test but only exercises `EnergyEngine`/`Fold` ordering in-process. This pass also confirmed two test-boundary gaps: some gravity tests already assert current `data_pipeline` / `binary_analysis` names while several kernel fiber tests still pin older `DA-*` / `data` nomenclature, and the current loader coverage only proves IP-anchored `events/` loading rather than hostname-identity loading from IP-shaped discovery artifacts. |
| UI | `deep_inspected` | `ui/index.html`, `ui/app.js`, `ui/styles.css` | UI is operational and daemon-backed. |
| Training / catalog / legacy domain helpers / modes | `mixed` | `training/__init__.py`, `training/trainer.py`, `training/scheduler.py`, `training/scheduler_main.py`, `training/corpus.py`, `catalog/__init__.py`, `catalog/compiler.py`, `domains/supply_chain/sensor.py`, `domains/supply_chain/nodes.py`, `modes/__init__.py` | Training is real hook-driven machinery with scheduler, corpus, and fine-tune orchestration, but its operator surface is uneven: scheduler docstrings advertise `skg train run` even though the CLI defines no `train` command, and repo-wide test search in this pass found no focused direct coverage of `skg.training.*`. The standalone YAML catalog compiler is also secondary to the live toolchain flow: it writes `attack_preconditions_catalog.v1.{domain}.json`, which diverges from the repo-wide `attack_preconditions_catalog.{domain}.v1.json` naming convention used by checked-in toolchains, generator output, and CLI paths. |
| `skg_deploy/` | `non_canonical` | `skg_deploy/ARCHIVED.md` and sampled files | Explicit archived deployment mirror. |
| `forge_staging/` and backup trees | `mixed` | `forge_staging/skg-nginx-toolchain/adapters/ssh_collect/parse.py`, `forge_staging/skg-nginx-toolchain/projections/nginx/run.py`, `forge_staging/skg-web-toolchain/adapters/ssh_collect/parse.py`, `forge_staging/skg-web-toolchain/projections/web/run.py`, staged `__init__.py` stubs | Still non-canonical, but this pass line-by-line inspected the remaining staged Python files. The staged SSH parsers are template-level and currently emit `unknown`/`not collected` because check keys do not match collected keys. |
| `Silver/` legacy scanner | `non_canonical` | `Silver/silver.py`, `Silver/core/*.py`, `Silver/modules/*.py`, package `__init__.py` stubs | Legacy auxiliary scanner; line-by-line pass found brittle resolver and Shodan no-info handling. Keep out of canonical runtime assumptions unless intentionally revived. |

## Validation Performed

- `pytest --collect-only -q` succeeded and collected `237` tests.
- Focused regression subset passed:
  - `project_event_file_keeps_distinct_attack_paths_per_run`
  - `same_domain_inference_requires_explicit_ad_metadata`
  - `sensor_loop_serializes_overlapping_sweeps`
  - `data_sensor_registers_same_database_bond_with_explicit_weight`
- Focused assistant/proposal regressions passed:
  - `contract_backed_msf_action_proposal_writes_artifact_and_metadata`
  - `cred_reuse_uses_contract_backed_action_proposal`
  - `daemon_assistant_demands_and_draft_endpoint`
  - `emit_auxiliary_proposals_uses_contract_backed_helper`
  - `execute_triggered_proposals_marks_missing_rc_and_records_failure`
- Re-ran focused runtime regressions:
  - `test_project_event_file_keeps_distinct_attack_paths_per_run`
  - `test_sensor_loop_serializes_overlapping_sweeps`
- Targeted import validation confirmed current `install_layer4.sh` smoke imports fail for `Ingester`, `SurfaceBuilder`, `ForgeCompiler`, `GapDetector`, and `CatalogCompiler`.
- Targeted projector validation confirmed the checked-in web projector only resolves `web_initial_access_v1`; current runtime strings `web_surface_v1`, `web_sqli_to_shell_v1`, and `web_full_chain_v1` did not resolve against the checked-in web catalog.
- Targeted runtime validation confirmed `collect_host()` now returns `False` when `SshSensor.run()` returns `[]` and `True` only when events are emitted.
- Targeted runtime validation confirmed `WEB-*` events remain `indeterminate` against the checked-in web projector/catalog while equivalent `WB-*` events realize successfully for `web_initial_access_v1`.
- Targeted inventory validation confirmed the live domain registry currently reports `12` domains and that `web` resolves to default path `web_initial_access_v1`.
- Targeted runtime confirmation showed `BondState.from_type(..., "docker_host").prior_influence == 0.9`, contradicting the `install.sh` smoke expectation of `0.45`.
- Targeted runtime validation confirmed sweep-scoped globbing still misses live sensor output: with one SSH run and one web run, emitted files used an independent UUID suffix (`..._10_0_0_9_<uuid>.ndjson`) or no run-id suffix (`..._web_web__demo.local_80.ndjson`), while `*_{sweep_run_id}.ndjson` matched none.
- Targeted catalog inventory confirmed the checked-in web catalog currently defines exactly `4` attack paths and `20` wickets.
- Targeted runtime-code inventory confirmed `skg-gravity/gravity_field.py` currently references `34` distinct `WB-*` wickets.
- Targeted synthetic projection validation confirmed a collector-style `WB-05` event with detail `Sensitive paths accessible` still realizes the checked-in `web_initial_access_v1` path, because the checked-in projector only keys on wicket ids and the checked-in catalog defines `WB-05` as `admin_interface_exposed`.
- Targeted projection validation confirmed daemon-web-sensor-style `WEB-*` events on `web_surface_v1` produce no web interpretation output.
- Targeted identity parsing validation confirmed raw host workload ids and prefixed workload ids (`ssh::...`, `host::...`, `web::...`) share the same `identity_key` but produce different `manifestation_key` values.
- Repo-wide search in this pass found no live read-side callsites for `skg/core/state_db.py` query helpers outside that module, even though later passes found write-side construction and inserts in `skg-gravity/gravity_field.py`.
- Existing tests cover domain-registry inventory discovery and decomposed gravity helper behavior, including measured-view domains, observed-tool aggregation, and cold-start ranking.
- Repo-wide search in this pass confirmed focused regression coverage now exists for `collect_host()` zero/positive-event semantics and injected-target interval bypass, but direct daemon `/collect` endpoint integration and `since_run_id` sweep-filtering behavior remain under-covered.
- Repo-wide search in this pass found no focused tests for web workload-id shape drift or for semantic alignment between active web collector wickets and the checked-in web catalog.
- Repo-wide search in this pass found no direct tests for `skg.training.corpus`, `skg.training.scheduler`, or `skg.training.trainer`.
- Repo-wide search in this pass found `skg.intel.engagement_dataset` referenced only in CLI mocking coverage, not in direct dataset-build or integrity-analysis tests.
- Targeted code inventory confirmed `skg.forge.proposals.accept()` and `reject()` each invoke the training corpus hooks twice.
- Targeted CLI inventory confirmed `skg.cli.app` defines no `train` command and no `data redteam` subcommand, despite those entrypoints being advertised in module docstrings.
- Targeted isolated runtime validation confirmed `build_engagement_db(..., delta_dir=None)` ingests `0` transitions while the same temp data with `delta_dir=DELTA_DIR` ingests transitions correctly.
- Targeted parser inspection confirmed the current top-level command set has no `train` entry, and the current `data` subcommands are only `catalog`, `discover`, `paths`, `profile`, and `project`.
- Repo-wide search in this pass found no direct behavioral tests for `cmd_report`, `cmd_calibrate`, or `cmd_proposals`; `cmd_engage` only has a missing-subcommand usage test in `tests/test_cli_commands.py`.
- Targeted host-toolchain inspection confirmed `skg-host-toolchain/contracts/catalogs/attack_preconditions_catalog.host.v1.json` defines multiple host attack paths and that `skg-host-toolchain/projections/host/run.py` itself defaults to `host_ssh_initial_access_v1`, while `skg/cli/commands/proposals.py` hardcodes `host_linux_privesc_sudo_v1` in `_run_post_projection()`.
- Targeted shell validation confirmed `"toolchain_generation"[:14] == "toolchain_gene"`, so the `cmd_proposals` list view comparison against `"toolchain_gener"` can never succeed.
- Repo-wide search in this pass found no direct behavioral tests for `cmd_surface`, `cmd_web_view`, `cmd_status`, `cmd_start`, `cmd_stop`, `cmd_mode`, `cmd_identity`, `cmd_field`, or `cmd_gravity`.
- Repo-wide search in this pass found direct tests for `cmd_check` and `cmd_replay`, but they remain shallow command-shell tests rather than behaviorally deep substrate/authority regressions.
- Repo-wide search in this pass found no direct tests for `skg-host-toolchain/adapters/msf_session/parse.py`.
- Targeted import validation confirmed the environment imports `winrm` successfully while `__import__('pywinrm')` fails, so `cmd_check` currently produces a false warning/failure mode for WinRM support.
- Targeted helper validation confirmed `_choose_fold_rows()` and `_choose_fold_summary()` prefer offline fold state over daemon results whenever the offline set is larger by count, regardless of freshness.
- Targeted `cmd_surface` validation with two targets and two interp files sharing one `attack_path_id` produced only one displayed projection row, confirming that the surface projection summary collapses results by attack-path id across targets.
- Targeted offline execution of `skg-host-toolchain/adapters/msf_session/parse.py` confirmed emitted `HO-17` / `HO-10` events use raw `workload_id` values like `10.0.0.7`, not `host::10.0.0.7`.
- Line-by-line read of `tests/test_runtime_regressions.py` in this pass confirmed direct regressions for assistant custody gating, binary identity aliasing, registry-driven projector discovery, observed-tool overlays on surface views, and dynamic sensor module discovery.
- Repo-wide search in this pass found direct tests for `cmd_graph`, `cmd_feed`, and `cmd_core`, but no direct tests for `cmd_resonance`, `cmd_derived`, `cmd_aprs`, `cmd_escape`, `cmd_lateral`, or `cmd_catalog`.
- Targeted parser validation confirmed `skg field binary::192.168.254.5::ssh-keysign binary` and `skg field data::users data_pipeline` currently fail parser validation, while `skg field host::10.0.0.7 host` parses successfully.
- Targeted helper validation confirmed `_rebuild_interp_from_events()` projects both `SKG_STATE_DIR/events` and `DISCOVERY_DIR`, so `skg derived rebuild` is not currently limited to append-only substrate inputs.
- Targeted helper validation confirmed `dark_hypothesis_sensor._available_instruments()` ignores built-in style toolchains that exist outside `SKG_STATE_DIR/toolchains`.
- Targeted code cross-reference confirmed `cognitive_action` proposals created by `dark_hypothesis_sensor` are list/show-visible but have no shared `trigger` path and would hit the generic `accept()` missing-`staged_path` failure path.
- Targeted parser validation confirmed `skg resonance draft-prompt ...` and `skg resonance draft-accept ...` currently fail parser validation even though `draft_catalog()` instructs operators to use them when no backend is available.
- Targeted helper validation confirmed `draft_catalog(..., api_key=None)` with Ollama and pool unavailable raises a `ValueError` and creates no prompt artifacts, despite module text describing prompt mode as the default no-key path.
- Targeted helper validation confirmed `draft_prompt()` and `draft_accept()` themselves do work as file-based helpers when invoked directly from Python, so the main mismatch is operator exposure rather than helper absence.
- Repo-wide search in this pass found no direct CLI coverage for `resonance draft` / `drafts`, no coverage for prompt-mode helper exposure, and no direct tests for `OllamaBackend.available()`, `list_models()`, `model()`, `generate()`, or `status()`.
- Targeted helper validation confirmed `draft_prompt()` currently fails against the real `ResonanceEngine.surface()` data shape with `TypeError: string indices must be integers, not 'str'`, so the drafting mismatch is not only CLI exposure drift.
- Targeted runtime inspection confirmed `ResonanceEngine` has no `list_drafts()` method even though `skg/resonance/cli.py` calls it for the standalone `drafts` command.
- Targeted ingester validation confirmed `ingest_all()` leaves `DomainMemory.adapters == []` for discovered domains and increments `domains_added` even when `store_domain()` returns `False`.
- Repo-wide search in this pass found no direct tests for `draft_prompt()`, `draft_accept()`, standalone `skg.resonance.cli`, `ResonanceEngine.surface()` / `save_draft()`, or resonance ingester adapter/domain completeness.
- Targeted observation-memory validation confirmed `historical_confirmation_rate(..., workload_id=...)` still mixed realized and blocked records from different workloads when the wicket id matched exactly, because `recall()` accepts same-wicket records before identity filtering.
- Line-by-line read of `tests/test_sensor_projection_loop.py` confirmed current coverage only proves shared-identity aliasing (`ssh::...` vs `host::...`) and does not pin separation across different identities for the same wicket/domain.
- Targeted embedder validation confirmed the TF-IDF fallback changes a text's vector after corpus growth, and targeted `MemoryStore` validation confirmed append-only indexing assumes a stable embedding basis.
- Repo-wide search in this pass found no direct tests for `skg.resonance.embedder` or for observation-memory separation across different workloads sharing one wicket/domain.
- Targeted temporal validation confirmed `DeltaStore.calibrate_confidence_weights()` returns only default `insufficient_data` weights even after ingesting a real `unknown`→`realized` transition, because current `WicketTransition` records do not supply any usable `evidence_rank`.
- Repo-wide naming search confirmed `skg.catalog.compiler.compile_file()` writes `attack_preconditions_catalog.v1.{domain}.json`, while the live toolchain/runtime convention elsewhere in the repo is `attack_preconditions_catalog.{domain}.v1.json`.
- Repo-wide callsite search found no live references to `skg-gravity/gravity_web.py` helpers or to `skg-gravity/exploit_proposals.py`.
- Repo-wide search in this pass found no direct tests for `DeltaStore.calibrate_confidence_weights()`, `skg.catalog.compiler`, `gravity_web.py`, or `exploit_proposals.py`.
- Targeted event-shape validation confirmed `db_discovery.check_de_01()` emits events with no `provenance` block and no `attack_path_id` in payload.
- Targeted adapter validation confirmed a monkeypatched HTTP-only IoT target still yields only `IF-01` `unknown`, because `probe_device()` returns before any HTTP probe when `probe_tcp_banner()` produces no banner.
- Targeted root-projector validation confirmed `skg-web-toolchain/projections/run.py` `latest(..., workload_id=...)` ignores `workload_id` and returns the last matching `attack_path_id` regardless of workload.
- Targeted path validation confirmed `skg-web-toolchain/projections/run.py` points at a non-existent `web_attack_preconditions_catalog.v1.json`.
- Repo-wide search in this pass found no direct tests for `skg-ai-toolchain/adapters/ai_probe/probe.py`, `skg-iot_firmware-toolchain/adapters/firmware_probe/probe.py`, `skg-nginx-toolchain/projections/nginx/run.py`, `skg-metacognition-toolchain/projections/metacognition/run.py`, or the stale root `skg-web-toolchain/projections/run.py`.
- Line-by-line toolchain read in this pass confirmed `skg-iot_firmware-toolchain/adapters/firmware_probe/__init__.py` and `probe.py` are materially different adapter implementations with different default attack-path ids and collection modes.
- Targeted path-resolution validation confirmed `Path(__file__).resolve().parents[4]` evaluates to `/opt` for `skg-host-toolchain/projections/host/run.py`, `skg-ad-lateral-toolchain/projections/lateral/run.py`, `skg-iot_firmware-toolchain/projections/iot_firmware/run.py`, `skg-data-toolchain/projections/data/run.py`, and `skg-container-escape-toolchain/projections/escape/run.py`, so their optional sheaf-import shim cannot locate the repo root as written.
- Line-by-line toolchain read confirmed `skg-aprs-toolchain/skg.py` and `skg-aprs-toolchain/projections/aprs/run.py` still default to `contracts/catalogs/attack_preconditions_catalog.v1.json` rather than the repo-wide `{domain}.v1` naming pattern.
- Narrow golden validation passed for `skg-aprs-toolchain/tests/test_golden.py`, `skg-container-escape-toolchain/tests/test_golden.py`, and `skg-ad-lateral-toolchain/tests/test_golden.py`.
- Narrow golden validation also passed for `skg-host-toolchain/tests/test_golden.py`.
- Line-by-line fixture reads confirmed APRS golden coverage is only a three-event projector check, AD lateral golden coverage is BloodHound-only, and container-escape golden is the only one of those three that directly exercises both the checked-in adapter fixture and the projector.
- Repo-wide search in this pass found no direct behavioral tests for `skg-aprs-toolchain/adapters/config_effective/parse.py`, `skg-aprs-toolchain/adapters/net_sandbox/parse.py`, `skg-ad-lateral-toolchain/adapters/ldapdomaindump/parse.py`, or `skg-ad-lateral-toolchain/adapters/manual/parse.py`.
- Line-by-line reads confirmed `skg-aprs-toolchain/contracts/envelope/skg.event.envelope.v1.json`, `skg-ad-lateral-toolchain/contracts/envelope/skg.event.envelope.v1.json`, and `skg-container-escape-toolchain/contracts/envelope/skg.event.envelope.v1.json` are toolchain-local copies of the same event-envelope schema shape rather than a shared canonical contract file.
- Toolchain-root inventory in this pass found no local test files under `skg-ai-toolchain`, `skg-binary-toolchain`, `skg-iot_firmware-toolchain`, `skg-metacognition-toolchain`, `skg-nginx-toolchain`, `skg-supply-chain-toolchain`, or `skg-web-toolchain`; `skg-data-toolchain/tests/` currently contains only an empty `__init__.py`.
- Line-by-line reads of `skg-nginx-toolchain/forge_meta.json` and `skg-web-toolchain/forge_meta.json` confirmed those roots still advertise template-generated fallbacks or missing generation backend coverage.
- Line-by-line reads of `skg-supply-chain-toolchain/adapters/sbom_check/check.py` and `skg-supply-chain-toolchain/projections/supply_chain/run.py` confirmed one sharper code/catalog mismatch (`SC-10`) plus another instance of the brittle `parents[4]` sheaf-import shim.
- Line-by-line reads of `skg-nginx-toolchain/adapters/ssh_collect/parse.py` and `skg-nginx-toolchain/projections/nginx/run.py` confirmed the nginx toolchain is active but untested locally; its adapter also collects `http_error_page` via `curl -sI`, so the branch that claims to inspect error-page body text for version disclosure is unreachable as written.
- Line-by-line reads of `skg-metacognition-toolchain/adapters/*/parse.py` confirmed all three metacognition adapters emit nonstandard `obs.substrate.node` records with no top-level `id`, `source.id` rather than `source_id`, and `evidence_rank` nested under `provenance.evidence` rather than the more typical top-level provenance field.
- Targeted helper validation confirmed `skg-metacognition-toolchain/projections/metacognition/run.py` ignores recency for conflicting wicket states: an older `blocked` MC-01 event still overrides a newer `realized` MC-01 event because the projector uses fixed state priority rather than latest timestamp.
- Line-by-line reads of `skg-web-toolchain/adapters/web_active/auth_scanner.py`, `gobuster_adapter.py`, `nikto_adapter.py`, `sqlmap_adapter.py`, and `transport.py` confirmed the web helper surface itself continues the semantic split: helper mappings reuse checked-in `WB-*` ids for different meanings, and `gobuster_adapter.py`, `nikto_adapter.py`, and `sqlmap_adapter.py` emit manual event dicts without the normal shared `source`/`provenance` envelope.
- Line-by-line read of `skg-web-toolchain/adapters/ssh_collect/parse.py` confirmed the canonical web tree still contains a legacy `adapter.web_collect` path that emits `W-01..W-05` Apache/APR-specific wickets rather than the current `WB-*` vocabulary.
- Repo-wide reference search in this pass confirmed that legacy web SSH collector path currently surfaces only in itself, `forge_staging`, older evidence/doc artifacts, and not in active tests or other obvious runtime callsites.
- Line-by-line reads of `skg-ai-toolchain/adapters/ai_probe/probe.py`, `skg-binary-toolchain/adapters/capa_analysis/parse.py`, `skg-binary-toolchain/adapters/angr_symbolic/parse.py`, and `skg-binary-toolchain/adapters/frida_trace/parse.py` confirmed those auxiliary adapters use the normal measured event envelope and expose programmatic `run()` entry points, making them structurally cleaner than many other generated helper paths.
- Repo-wide search in this pass found no direct behavioral tests for `skg-ai-toolchain/adapters/ai_probe/probe.py`, `skg-binary-toolchain/adapters/capa_analysis/parse.py`, `skg-binary-toolchain/adapters/angr_symbolic/parse.py`, `skg-binary-toolchain/adapters/frida_trace/parse.py`, or `skg-web-toolchain/adapters/ssh_collect/parse.py`.
- Targeted monkeypatch validation in this pass confirmed `skg-gravity/gravity_field.py::_exec_ai_probe()` rewrites a valid AI event NDJSON file into summary-only rows like `{\"wicket_id\": \"AI-01\", \"status\": \"realized\", ...}` before projection.
- Line-by-line read of `skg/sensors/projector.py` confirmed those summary rows are inadmissible for projection because `project_event_file()` only accepts `obs.attack.precondition` envelopes with the normal event shape.
- Repo-wide search in this pass found no direct tests for `skg-gravity/gravity_field.py::_exec_ai_probe` or the gravity AI projection path.
- Targeted validation in this pass confirmed `_exec_post_exploitation(...)` raises `UnboundLocalError` before session discovery when the target lacks explicit OS metadata.
- Targeted projector validation in this pass confirmed a normal `obs.attack.precondition` event with `source.toolchain = "skg-gravity"` yields no interpretation output from `project_event_file(...)`.
- Targeted structural validation in this pass confirmed the `_exec_data_profiler()` source slice still contains an unreachable embedded BloodHound collector block and no nested `def _exec_bloodhound`.
- Targeted validation in this pass confirmed `_merge_configured_targets(...)` raises `AttributeError` on a list-root `targets.yaml`, even though other repo paths support that shape.
- Targeted validation in this pass confirmed `detect_instruments()` marks `bloodhound` unavailable when the CE URL is unreachable even with `NEO4J_PASSWORD` configured.
- Targeted validation in this pass confirmed `get_wicket_graph(force_rebuild=True)` currently leaves `DP-01`, `DE-01`, and `IF-01` with `domain == "unknown"` and omits `AI-01` entirely.
- Targeted validation in this pass confirmed `KernelStateEngine._infer_domain_wickets(...)` still groups active `AD-01`, `BA-01`, `DP-01`, `DE-01`, and `IF-01` wickets under `unknown`.
- Targeted validation in this pass confirmed `KernelStateEngine.field_locals(...)` groups those same active wickets into one `unknown` local when `domain_wickets` is omitted.
- Targeted validation in this pass confirmed `load_observations_for_node('db.internal', discovery_dir, events_dir)` returns `0` for an IP-named discovery artifact whose payload carries `identity_key='db.internal'`.
- Targeted validation in this pass confirmed `skg.gravity.selection._instrument_spheres(['BA-03','DP-01','IF-01','WB-01','HO-01'])` currently returns only `{'host', 'web'}`.
- Targeted validation in this pass confirmed `_observed_tooling_boost('binary_analysis', ...)` returns `0.0` when only `domain_hints=['binary']` are present for a `BA-03`-wavelength instrument.
- Targeted validation in this pass confirmed `domain_to_sphere('data_pipeline') == 'data_pipeline'`, and a `data_pipeline` local with topology keyed as `data` therefore misses topology contribution in `field_functional_breakdown(...)`.
- Targeted validation in this pass confirmed `coupling_value('web', 'data', table='inter_local') == 0.85` while `coupling_value('web', 'data_pipeline', table='inter_local') == 0.1`.
- Targeted validation in this pass confirmed `coupling_value('host', 'binary', table='inter_local') == 0.6` while `coupling_value('host', 'binary_analysis', table='inter_local') == 0.1`.
- Targeted validation in this pass confirmed `field_functional_breakdown([web, data_old]).coupling_energy == 1.53` while the same structure using `domain='data_pipeline'` drops to `0.2`.
- Targeted validation in this pass confirmed `surface()` currently returns `score == 0.0` and raw domain labels for explicit `domain="data_pipeline"` / `domain="binary_analysis"` projections even when the payload carries `data_score` / `binary_score`.
- Targeted validation in this pass confirmed `daemon._find_projection_files(...)` returns a `domain='data'` projection for `domain='data'` but returns `[]` for the same artifact when queried as `domain='data_pipeline'`.
- Targeted validation in this pass confirmed `anchored_field_pull(..., domains={'binary_analysis'}, sphere_pulls={'binary': 10.0}, ...) == 0.0`, while equivalent current-name cases for `data_pipeline`, `container_escape`, and `ad_lateral` still produce nonzero pull.
- Targeted validation in this pass confirmed `_world_states_from_surface(...)` given target domains `['data_pipeline','binary_analysis','container_escape','ad_lateral']` returns only `{'ad', 'container'}` and drops `data` / `binary`.
- Targeted validation in this pass confirmed daemon field-state computation calls `anchored_field_pull(...)` with `[]` for `binary_stack_overflow_v1` but with `['data_pipeline']` for `data_exposure_v1`.
- Targeted validation in this pass confirmed that same mocked field-state run yields `binary field_pull == 0.0` / `E == 1.0` while the comparable data path receives the mocked pull (`field_pull == 7.0`, `E == 8.0`).
- Repo-wide search in this pass confirmed daemon/topology alias coverage still pins mostly legacy names: `tests/test_runtime_regressions.py` covers only the binary alias case, and `tests/test_sensor_projection_loop.py` world/topology slices still center old `data` / `container` names.
- Repo-wide search in this pass found no direct behavioral tests for `_compute_field_state_inner()` or `get_projection_field(...)`; current field-state references in `tests/test_sensor_projection_loop.py` are fixture rows rather than live daemon field-state computation.
- Targeted validation in this pass confirmed `_identity_manifestations('10.0.0.7')` returns a binary manifestation with `score == 0.0` despite `binary_score == 0.82`, and `_identity_manifestations('corp.local')` returns an AD manifestation with `score == 0.0` despite `lateral_score == 0.73`.
- Line-by-line test review confirmed the main `_identity_world(...)` coverage path patches `_identity_manifestations` to `[]`, and repo-wide search in this pass found no direct behavioral tests for manifestation-score extraction in daemon world views.
- Targeted validation in this pass confirmed `_compute_field_state()` returns `{}` on the first cold-cache call when recompute is slow, then returns computed data on a later call after the background refresh completes.
- Repo-wide search in this pass found no direct tests for `status_refresh()`, stale-cache `/status`, or `_assistant_context(...)` under an empty field-state cache.
- Targeted validation in this pass confirmed `identity_timeline('db.internal')` returns `workload_count == 0` and empty history when `kernel.feedback.timeline(...)` is available but `INTERP_DIR` is empty.
- Targeted validation in this pass confirmed adding one interp payload for `host::db.internal` makes `identity_timeline('db.internal')` return the expected snapshot/transition rows.
- Repo-wide search in this pass found no direct tests for `identity_timeline(...)`, and the main assistant-context test still mocks `identity_timeline` rather than exercising the live discovery path.
- Targeted validation in this pass confirmed `_assistant_reasoning_bundle(...)` with `timeline["workloads"] = ['host::db.internal']` and no group/field rows produces an empty graph context because it does not seed from timeline workloads.
- Targeted validation in this pass confirmed the same bundle shape with `timeline["graph_neighbors"] = [{'workload_id': 'web::10.0.0.7:443', ...}]` calls `kernel.graph.neighbors('web::10.0.0.7:443')` instead of the selected identity workload.
- Repo-wide search in this pass found no direct tests for `_assistant_reasoning_bundle(...)`, and the main assistant-context test still mocks `identity_timeline(...)` rather than exercising live bundle workload seeding.
- Targeted validation in this pass confirmed `_assistant_context(...)` can return `field_state['count'] == 1` while `subject['paths'] == []` when `field_surface()` is empty.
- Targeted validation in this pass confirmed `_assistant_fallback({... task='target_summary'})` then reports `0 manifestations and 0 active attack-path rows` even though one field-state row exists.
- Repo-wide search in this pass confirmed the assistant summary tests still mock `_assistant_prepare_context(...)` and do not exercise this live mismatch between subject paths and field state.
- Line-by-line read in this pass confirmed `/assistant/explain` and `/assistant/what-if` both route through `_assistant_prepare_context(...)` and return `references.timeline`, `field_path_count`, and bundle-derived graph-neighbor counts directly from that prepared context, so the already-recorded timeline/graph/field-state defects propagate unchanged into the public assistant API surface.
- Line-by-line read in this pass confirmed `tests/test_sensor_projection_loop.py` still exercises `assistant_explain` and `assistant_what_if` by mocking `_assistant_prepare_context(...)`, so the live assistant endpoint wiring around field-state, timeline discovery, and graph-context seeding remains untested.
- Targeted validation in this pass confirmed `assistant_explain(...)` currently reports `references["graph_neighbor_count"]` from the truncated `bundle.graph.neighbors` list rather than `bundle.graph.neighbor_count`, and `references["artifact_count"]` from the truncated `context["artifacts"]` preview list rather than the bundle artifact count.
- Repo-wide search in this pass found no direct tests for assistant `references.artifact_count` or `references.graph_neighbor_count` semantics.
- Targeted validation in this pass confirmed `_identity_manifestations(...)` returns the first matching interp payload it sees for a `(workload_id, attack_path_id)` pair, not the newest one, by monkeypatching `INTERP_DIR.glob("*.json")` to return an older host projection before a newer one and observing the older `classification='indeterminate'` / `host_score=0.2` row win.
- Repo-wide search in this pass found no direct behavioral tests for `_identity_manifestations(...)`; the only opened world-view test still patches `_identity_manifestations` to `[]`.
- Targeted validation in this pass confirmed `_artifact_matches_identity(Path('observe_db_internal_noise.json'), 'db.internal')` returns `(True, None)` even when the payload carries `workload_id='host::other.internal'`, because filename-token matching short-circuits payload identity checks.
- Repo-wide search in this pass found no direct tests for `_artifact_matches_identity(...)` or `identity_artifacts(...)`.
- Targeted validation in this pass confirmed `_assistant_context(...)` ignores `identity_artifacts(...).count` and sets `bundle["artifacts"]["count"] == 2` when the mocked artifact source reported `count == 7` but returned only two preview rows.
- Repo-wide search in this pass found no direct tests for assistant bundle artifact-count semantics.
- Targeted validation in this pass confirmed `_identity_world(...)` reports `world_summary["manifestation_count"] == 2` when two attack-path rows share one `manifestation_key`, so the summary currently counts projection rows rather than unique manifestations.
- Repo-wide search in this pass found no direct assertions on `world_summary["manifestation_count"]`; the opened world-view test only checks credential/network summary fields and patches `_identity_manifestations` entirely.
- Targeted validation in this pass confirmed `_artifact_preview_payload(str(big.json), lines=1)` returns a JSON preview containing a `payload.big` string of length `5000`, so the public helper does not currently bound JSON object size.
- Repo-wide search in this pass found no direct tests for `_artifact_preview_payload(...)`, `_assistant_compact_artifact_preview(...)`, or the `/artifact/preview` endpoint.
- Line-by-line read in this pass confirmed `list_targets()` injects `_identity_world(...).get("world_summary", {})` directly into every target row, so the daemon-world manifestation-count overstatement is also visible in target-list responses, not just `/world/{identity_key}`.
- Line-by-line read in this pass confirmed the opened target-list test in `tests/test_sensor_projection_loop.py` patches `_identity_world` to `{"world_summary": {"service_count": 1}}`, so it does not exercise live `world_summary` counting semantics.
- Targeted validation in this pass confirmed one `list_targets()` row can simultaneously expose `manifestations == ['host::10.0.0.7']` from the measured-view index and `world_summary['manifestation_count'] == 2` from `_identity_world(...)`, so the inconsistency is visible inside a single target payload.
- Targeted validation in this pass confirmed `list_targets()` can also return `manifestations is None` while `world_summary['manifestation_count'] == 1` when `field_surface()` is empty but `_identity_world(...)` reports manifested state for the same identity.
- Targeted validation in this pass confirmed `skg.intel.surface.surface()` emits two workload rows with one shared `manifestation_key` when one manifestation has two attack paths, so the downstream target-list/world-summary mismatch is consistent with the current measured-surface shape rather than a one-off daemon bug.
- Targeted validation in this pass confirmed `_surface_subject_rows()` collapses those same two measured path rows into one identity row with one deduped manifestation key and merged realized/unknown counts, so CLI `surface`, `target list`, and `report` use a third aggregation shape between raw measured rows and daemon world-summary row counts.
- Repo-wide search in this pass found no direct tests for `_surface_subject_rows()` on one-manifestation/multi-path inputs; the opened CLI test covers only a single measured row and the opened target-list test still patches `_identity_world(...)`.
- Targeted validation in this pass confirmed `skg target list` prints an `E` column header while rendering `unknown_count` in that column, and the opened `test_target_list_prints_identity_first_rows` assertion only checks that the row prints at all rather than what the `E` column means.
- Targeted validation in this pass confirmed `cmd_surface` and `cmd_target(target_cmd='list')` can disagree on visible targets for the same latest-surface file because `surface` hydrates through `_hydrate_surface_from_latest_nmap(...)` while `target list` reads raw JSON directly; with a mocked hydrated target and empty on-disk JSON, `surface` printed `10.0.0.9  [database]  3306/mysql` while `target list` rendered only the empty table header.
- Repo-wide search in this pass found no opened direct command tests for `cmd_surface` or `cmd_report`; the opened CLI coverage only asserts that `cmd_target(target_cmd='list')` prints a row and does not exercise hydrated-surface parity across these commands.
- Targeted validation in this pass confirmed `_surface_subject_rows()` joins target metadata and measured rows only by exact `identity_key`: with an IP-root target (`10.0.0.9`) and a hostname-root measured row (`db.internal`), it produced two rows instead of one merged subject row. A direct `cmd_target(target_cmd='list')` validation showed the same split operator-facing output:
  - `db.internal             1                                data`
  - `10.0.0.9                0 3306/mysql                     data`
- Targeted validation in this pass confirmed daemon `list_targets()` has the same exact-key merge weakness: with `_all_targets_index()` returning one `10.0.0.9` target and `field_surface()` returning one `db.internal` measured row, `list_targets()` returned two target payloads instead of one merged identity row. The opened daemon test at `tests/test_sensor_projection_loop.py:3558-3611` explicitly asserts this split by requiring both `db.internal` and `10.0.0.7` rows.
- A follow-on daemon validation in this pass confirmed the split rows carry complementary partial state when `_identity_world(...)` runs for real: the `10.0.0.9` row retained `services=[{'port': 3306, 'service': 'mysql'}]` and `world_summary.manifestation_count == 0`, while the `db.internal` row retained `manifestations=['mysql::db.internal:3306::users']` and `world_summary.manifestation_count == 1` but had `services is None`.
- A further daemon/profile validation in this pass confirmed `_identity_profile(identity_key)` amplifies the same split: with a hostname-keyed `gravity_data_db.internal_*.ndjson` artifact carrying a `DP-10` observation for `mysql::db.internal:3306::users`, `_identity_profile('db.internal')` returned `datastore_access=['MySQL accessible as root — database access confirmed']` and `evidence_count == 1`, while `_identity_profile('10.0.0.9')` returned empty datastore evidence and `evidence_count == 0`. The opened world test at `tests/test_sensor_projection_loop.py:2235-2250` still patches `_identity_profile(...)`, so this live profile-matching seam is not directly covered.
- The next daemon alias pass confirmed local alias helpers already exist (`_identity_aliases`, `_identity_matches`) but `_identity_profile(...)` does not use them. A targeted validation with hostname-keyed `HO-13` SSH-key evidence showed `_identity_profile('db.internal')` returning `ssh_keys=['/home/app/.ssh/id_rsa']` while `_identity_profile('10.0.0.9')` returned `[]`; `_identity_relations('db.internal', {'ip': '', 'host': 'db.internal'})` then returned `[]` while `_identity_relations('10.0.0.9', {'ip': '10.0.0.9'})` still emitted only `same_subnet` against `10.0.0.8`. Repo-wide search in this pass found no opened direct tests for `_identity_relations(...)` or daemon shared-credential relation behavior.
- The follow-on artifact/timeline pass confirmed the same exact-key split reaches daemon history helpers too: with hostname-root `mysql::db.internal:3306::users` artifacts and a mocked feedback backend, `identity_artifacts('db.internal')` returned both the interp and event files while `identity_artifacts('10.0.0.9')` returned none; `identity_timeline('db.internal')` returned one workload and one snapshot while `identity_timeline('10.0.0.9')` returned zero workloads and zero snapshots. Repo-wide search in this pass found no opened direct tests for `identity_artifacts(...)` or `identity_timeline(...)`.
- The next assistant-context pass confirmed the same exact-key identity split reaches assistant selection: `_assistant_context()` builds `targets_by_identity` and measured groups by exact identity string, while `identity_world()` already resolves target rows through `_identity_matches(...)`. A targeted validation with split daemon rows (`10.0.0.9` service row, `db.internal` manifestation row) showed `_assistant_context(kind='target', id='10.0.0.9', identity_key='10.0.0.9')` producing `subject.manifestations=[]`, `subject.paths=[]`, and `field_path_count == 0` even though the measured field row existed under `db.internal`. The opened assistant-context regression at `tests/test_sensor_projection_loop.py:2895-3052` uses one fully aligned identity (`10.0.0.7`) across target rows, field rows, timeline, and artifacts, so it does not exercise alias-split assistant selection.
- The follow-on world-endpoint pass confirmed `identity_world()` is only half alias-aware: it selects a target row through `_identity_matches(...)` but still calls `_identity_world(requested_identity, target)`. A targeted validation with request id `10.0.0.9`, a matched target row rooted at `db.internal`, and a hostname-root interp payload produced a world response with `service_count == 1`, `services=[{'port': 3306, 'service': 'mysql'}]`, but `manifestation_count == 0` and `manifestations == []`. Repo-wide search in this pass found no opened direct tests for the `/world/{identity_key}` endpoint path; the only opened world test exercises `_identity_world(...)` directly with a fully aligned identity.
- A follow-up endpoint validation in this pass made the call mismatch explicit: with `identity_world('10.0.0.9')`, a matched target row rooted at `db.internal`, and `_identity_world(...)` monkeypatched to echo its arguments, the endpoint passed `identity_key_seen='10.0.0.9'` alongside `target_seen.identity_key='db.internal'`, confirming that alias matching happens for target selection but not for world-state identity resolution.
- A follow-on daemon field-state hint validation in this pass confirmed `_resolution_hint(...)` maps web guidance under `WE` only; `_resolution_hint('WB-01')` and `_resolution_hint('WEB-01')` both returned the generic fallback text, and `_compute_field_state_inner()` carried that same generic hint into `resolution_required` for unknown `WB-*` web wickets.
- Targeted validation in this pass confirmed `DataSensor` currently ships with unresolved private-attribute paths: `hasattr(sensor, '_events_dir') == False`, `hasattr(sensor, '_graph') == False`, direct `_register_bonds()` raises `AttributeError`, and `run()` with a stubbed `profile_table(...)` raises `AttributeError: 'DataSensor' object has no attribute '_events_dir'`.
- Targeted validation in this pass confirmed `zero_day_detector.SKG_HOME` resolves to `/opt` from the live module path, so catalog write roots point outside `/opt/skg`.
- Targeted validation in this pass confirmed `detect_version_gaps(...)` treats `domain_wickets` as description-like strings: with `{'web': {'WB-01'}}` it still reports an `apache/2.4.49` version gap, while a synthetic description token containing `apache 2.4.49` suppresses it.
- Targeted validation in this pass confirmed `probe_process_surface(...)` emits literal conditional-template text for `PR-03`, `PR-09`, and `PR-10` details (e.g., `{'ASLR off ...' if aslr_disabled else 'ASLR enabled'}`) due split f-string literals.
- Targeted validation in this pass confirmed `MsfSensor._drain_sessions(...)` emits session events even when `seen_sessions` already contains the session key (`len(first)==1`, `len(second)==1`), so persisted session state currently does not dedupe repeat emission.
- Targeted validation in this pass confirmed `impacket_post._emit(...)`, `openssl_tls.run(...)`, `smbclient.run(...)`, and `ldap_enum.run(...)` all currently hit `TypeError: envelope() missing 1 required positional argument: 'pointer'` on reachable event paths.
- Targeted runtime validation in this pass confirmed `db_profiler.profile_table(..., attack_path_id='custom_path_v9')` emits events with no `payload.attack_path_id`, so caller-provided path ids are currently dropped.
- Targeted validation in this pass confirmed `skg-binary-toolchain/adapters/binary_analysis/__init__.py` inserts `/opt/skg-gravity` (non-existent) rather than `/opt/skg/skg-gravity` into `sys.path`.
- Repo-wide search in this pass found no direct tests for `_exec_post_exploitation()`, gravity plugin adapters `ldap_enum.py`, `openssl_tls.py`, `smbclient.py`, or gravity-cycle identity/delta-E accounting.
- Repo-wide search in this pass found no direct tests for the gravity SSH duplicate-write path, the gravity sysaudit projection path, or the live `_exec_bloodhound()` shell path.
- Repo-wide search in this pass found no direct tests for `BloodHoundSensor`, `collect_via_api()` domain scoping, or gravity availability decisions around Neo4j fallback.
- Repo-wide search in this pass found no direct tests for `get_wicket_graph()` against the current AI/data/IoT catalog inventory or for explicit-domain `surface()` score-key alignment.
- Repo-wide search in this pass found no direct behavioral tests for `skg-host-toolchain/adapters/ssh_collect/parse.py`, `winrm_collect/parse.py`, or `nmap_scan/parse.py`; current host golden coverage only exercises the projector against a fixture NDJSON.
- Line-by-line read confirmed `skg-host-toolchain/adapters/ssh_collect/parse.py` contains a duplicated unreachable tail block after `return 0` in `main()`.
- Targeted validation in this pass confirmed `feeds/nvd_ingester.ingest_from_surface(...)` currently dedupes by service string across all targets: with two targets exposing `Apache/2.4.49`, monkeypatched `ingest_service(...)` was called once for the first target only.
- Targeted runtime validation in this pass confirmed `python skg_paper_evidence.py --out /tmp/skg_evidence_run --quiet` fails section `C_propagation` with `'WorkloadGraph' object has no attribute 'INTRA_TARGET_COUPLING'`.
- Targeted validation in this pass confirmed staged parsers `forge_staging/skg-nginx-toolchain/adapters/ssh_collect/parse.py` and `forge_staging/skg-web-toolchain/adapters/ssh_collect/parse.py` emit only `unknown`/`not collected` under their own default collection keys.
- Targeted validation in this pass confirmed `Silver/core/resolver.py` returns empty for `resolve('example.com')` and `Silver/modules/shodan.py` raises `KeyError: 'vulns'` on InternetDB-style no-info payloads.
- No broad runtime validation, live target collection, destructive actions, or networked installs were performed in this pass.

## Review Boundary

This pass is sufficient for:

- repo-wide architecture understanding
- doc/code alignment analysis
- naming drift detection
- test/validation gap mapping
- planning the next repair sequence

This pass is not sufficient for:

- certifying end-to-end daemon correctness
- certifying generated auxiliary toolchains as runnable
- certifying install/bootstrap scripts on a fresh host
- certifying live target collection behavior beyond static inspection and narrow tests
