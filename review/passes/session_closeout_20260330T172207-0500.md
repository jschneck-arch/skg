# Session Closeout 20260330T172207-0500

## Scope

Continued the read-first audit in the gravity execution shell and adjacent helper paths. This slice focused on the previously unread middle span of `skg-gravity/gravity_field.py` plus the helper modules needed to validate new shell-level findings.

## Files Opened

- `skg-gravity/gravity_field.py` lines `4986-6869`
- `skg-gravity/gravity_field.py` lines `2320-2415`
- `skg-gravity/adapters/__init__.py`
- `skg-gravity/cred_reuse.py`
- `skg/sensors/adapter_runner.py`
- `skg-host-toolchain/adapters/sysaudit/audit.py`
- `review/CANONICAL_MODEL.md`
- `review/REVIEW_LEDGER.md`
- `review/OPEN_ISSUES.md`

## Narrow Validation

- Confirmed `_exec_post_exploitation(...)` still raises `UnboundLocalError` when target OS metadata is absent.
- Confirmed a normal `obs.attack.precondition` event with `source.toolchain = "skg-gravity"` produces no interpretation output from `project_event_file(...)`.
- Confirmed the `_exec_data_profiler()` source slice still contains the embedded BloodHound collector body after its `return`, and that this is not a nested function definition.

## Findings Added This Slice

1. `_exec_post_exploitation()` can crash before session discovery when target OS metadata is absent.
2. Gravity-generated host and web events still overload canonical wicket ids with conflicting meanings.
3. Several gravity plugin adapters emit non-projectable `toolchain="skg-gravity"` events.
4. `gravity_field_cycle` loses selected subject identity and compares pre/post energy with different term sets.
5. `_exec_data_profiler()` still contains a dead embedded BloodHound collector body after its `return`.
6. `_exec_ssh_sensor()` passes an `out_file` into `run_ssh_host()` and then appends the returned events back into that same file, duplicating host observations.
7. `_exec_sysaudit()` writes raw event files but does not project them in the same gravity cycle.

## Artifact Updates

- Updated `review/CANONICAL_MODEL.md`
- Updated `review/REVIEW_LEDGER.md`
- Updated `review/OPEN_ISSUES.md`
