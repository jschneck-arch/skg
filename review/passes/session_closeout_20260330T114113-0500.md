# Session Closeout — 20260330T114113-0500

## Scope

Deeper file-by-file pass over the next highest-risk runtime paths:

- explicit single-target collection
- run-scoped auto-projection
- split web observation/projector/catalog/gravity paths
- installer/bootstrap smoke drift

## Files Inspected In Detail

- `skg/sensors/__init__.py`
- `skg/sensors/ssh_sensor.py`
- `skg/sensors/projector.py`
- `skg/sensors/web_sensor.py`
- `skg/sensors/struct_fetch.py`
- `skg/core/daemon.py`
- `skg/cli/commands/target.py`
- `skg/cli/commands/proposals.py`
- `skg-web-toolchain/adapters/web_active/collector.py`
- `skg-web-toolchain/projections/web/run.py`
- `skg-web-toolchain/projections/run.py`
- `skg-web-toolchain/contracts/catalogs/attack_preconditions_catalog.web.v1.json`
- `skg-gravity/gravity_field.py`
- `skg/topology/energy.py`
- `skg/intel/surface.py`
- `skg/core/domain_registry.py`
- `install.sh`
- `install_layer4.sh`
- `setup_arch.sh`
- relevant tests and targeted search results around these paths

## Targeted Validation Performed

### Single-target collection success semantics

Executed a narrow mocked runtime check:

- patched `SshSensor.run()` to return `[]`
- called `collect_host(...)`
- result: `True`

This confirms `collect_host()` reports success even when no events are emitted.

### Web namespace / projector compatibility

Executed the checked-in nested web projector directly:

- `WEB-01` + `WEB-05` events against `web_initial_access_v1` -> `indeterminate`
- `WB-01` + `WB-05` events against `web_initial_access_v1` -> `realized`

This confirms the namespace mismatch is operational, not just nominal.

### Domain inventory

Executed `summarize_domain_inventory()`:

- current inventory count -> `12`
- `web` default path -> `web_initial_access_v1`

This confirms the current registry view disagrees with several live producer defaults.

### Run-scoped auto-projection

Executed a narrow filename-pattern check:

- created `20260330_web_host.ndjson`
- called `project_events_dir(..., since_run_id='run-1')`
- result: zero matches, because current filenames do not include the run id suffix the projector expects

### Installer smoke imports

Validated the remaining `install_layer4.sh` class imports:

- `skg.intel.gap_detector.GapDetector` -> missing
- `skg.catalog.compiler.CatalogCompiler` -> missing

## New Concrete Findings

1. Explicit single-target collection is more broken than the prior pass showed.
   - `collect_host()` sets `_force_collect`, but no runtime code reads it.
   - `SshSensor.run()` still applies interval gating.
   - `collect_host()` still returns success on zero events.

2. Run-scoped auto-projection is not actually run-scoped.
   - emitted event filenames do not carry the sweep `run_id`
   - projector filtering expects them to
   - sensor loop compensates by falling back to a recent full scan

3. The web domain is split across multiple incompatible execution paths.
   - daemon sensor path emits `WEB-*`
   - direct web collector path emits `WB-*`
   - direct collector also emits `WB-21..WB-24`, which the checked-in catalog does not define
   - `struct_fetch` emits `WB-30..WB-40`, which the checked-in catalog does not define
   - gravity logic reasons over the broader `WB-*` space through `WB-40`
   - direct CLI web observe writes to `DISCOVERY_DIR`, not the canonical measured event path

4. Installer/bootstrap drift is broader than previously recorded.
   - `install_layer4.sh` expects five missing class names, not three
   - both `install.sh` and `setup_arch.sh` repeat the obsolete bond smoke assertion

## Artifact Updates In This Pass

- updated `review/CANONICAL_MODEL.md`
- updated `review/REVIEW_LEDGER.md`
- updated `review/OPEN_ISSUES.md`
- added `review/passes/session_closeout_20260330T114113-0500.md`

## Recommended Immediate Next Work

1. Inspect and specify one canonical web observation path before any web repair attempt.
2. Inspect `skg/sensors/projector.py` and `skg/sensors/__init__.py` together for run-boundary semantics before changing auto-projection behavior.
3. Treat installer/bootstrap scripts as untrusted until their smoke expectations are reconciled against live module APIs.
4. Keep actual fixes sequenced after the already-identified `/collect` and web-path repairs, since those still dominate correctness risk.
