# Session Closeout: 2026-03-30T16:51:09-0500

## Scope

Continued the file-by-file audit through native-toolchain catalogs, host golden fixtures, and small toolchain requirements files.

Files opened in this slice:

- `skg-host-toolchain/contracts/catalogs/attack_preconditions_catalog.host.v1.json`
- `skg-host-toolchain/tests/golden/events/sample.ndjson`
- `skg-host-toolchain/tests/golden/expected/expected_payload.json`
- `skg-ad-lateral-toolchain/contracts/catalogs/attack_preconditions_catalog.ad_lateral.v1.json`
- `skg-aprs-toolchain/contracts/catalogs/attack_preconditions_catalog.v1.json`
- `skg-container-escape-toolchain/contracts/catalogs/attack_preconditions_catalog.container_escape.v1.json`
- `skg-host-toolchain/requirements.txt`
- `skg-ad-lateral-toolchain/requirements.txt`
- `skg-aprs-toolchain/requirements.txt`
- `skg-container-escape-toolchain/requirements.txt`

## Findings

1. The checked-in host golden fixture is extremely narrow.
   - It contains only `HO-03` and `HO-06` for `host_linux_privesc_sudo_v1`.
   - The expected payload only asserts `attack_path_id`, wickets, score, and classification.

2. Host catalog prose has drifted behind live adapter evidence ranks.
   - The host catalog still frames `HO-01`, `HO-02`, and `HO-04` as minimum rank-4 network evidence.
   - Live SSH and WinRM adapters emit stronger rank-1 confirmations for those conditions after successful authentication.

3. APRS and container escape catalogs remain internally coherent with the paths already validated earlier.
   - APRS stays on the older `attack_preconditions_catalog.v1.json` naming convention.
   - Container escape remains aligned with its checked-in adapter/projector pair.

4. Toolchain requirements are small and uneven.
   - APRS, AD lateral, and container escape depend only on `jsonschema` in-tree.
   - Host depends on `paramiko`, `pywinrm`, `pymetasploit3`, and `pyyaml`.

## Validation

Already-confirmed host golden remained the operative validation boundary for this slice:

- `python /opt/skg/skg-host-toolchain/tests/test_golden.py`

No additional runtime fixes or bootstrap installs were performed.

## Artifact Updates

Updated:

- `review/REVIEW_LEDGER.md`
- `review/OPEN_ISSUES.md`

No fixes were applied.
