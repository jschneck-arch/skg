# Session Closeout

Timestamp: 2026-03-31T08:48:30-0500

Scope:

- continued the measured-surface / target-list manifestation audit
- re-read `skg/intel/surface.py`
- read `_surface_subject_rows()` in `skg/cli/utils.py`
- re-read CLI consumers in `skg/cli/commands/surface.py`, `target.py`, and `report.py`
- re-read the opened CLI/surface tests that currently cover this seam

What was confirmed:

- `skg.intel.surface.surface()` intentionally emits one workload row per projected attack path while preserving `manifestation_key`
- `_surface_subject_rows()` then groups those measured path rows by `identity_key` and dedupes manifestation keys into one subject row
- CLI `surface`, `target list`, and `report` all consume that identity-grouped subject-row view
- daemon `_identity_world()` still counts path-shaped manifestation rows in `world_summary["manifestation_count"]`
- the manifestation mismatch is therefore structural across three operator aggregation shapes:
  - raw measured surface path rows
  - CLI identity-grouped subject rows with deduped manifestations
  - daemon world summaries that still count path rows as manifestations

Targeted validation:

- built a temporary interp dir with two projected paths on one manifestation
- confirmed `surface()` returned `2` workload rows with one shared `manifestation_key`
- confirmed `_surface_subject_rows()` collapsed those into `1` identity row with:
  - `manifestations == ['host::10.0.0.7']`
  - `realized_count == 2`
  - `unknown_count == 1`

Review artifact updates:

- `review/CANONICAL_MODEL.md`
- `review/REVIEW_LEDGER.md`
- `review/OPEN_ISSUES.md`

No code fixes were applied.
