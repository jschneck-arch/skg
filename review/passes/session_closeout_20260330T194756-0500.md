# Session Closeout 20260330T194756-0500

## Scope

Continued the read-first audit from kernel support into observation loading, field-local construction, field-functional coupling, and the decomposed gravity consumers that depend on those layers.

## Files Opened

- `skg/kernel/adapters.py`
- `skg/kernel/observations.py`
- `skg/kernel/field_local.py`
- `skg/kernel/field_functional.py`
- `skg/kernel/contexts.py`
- `skg/kernel/projections.py`
- `skg/kernel/reason.py`
- `skg/gravity/selection.py`
- `skg/gravity/landscape.py`
- `tests/test_sensor_projection_loop.py` (kernel/gravity naming slices)
- `tests/test_runtime_regressions.py` (loader coverage slice)
- `review/CANONICAL_MODEL.md`
- `review/REVIEW_LEDGER.md`
- `review/OPEN_ISSUES.md`

## Narrow Validation

- Confirmed `load_observations_for_node('db.internal', ...)` returns `0` when the only matching discovery artifact is IP-named (`gravity_http_10_0_0_7_*.ndjson`) even though the payload identity is `db.internal`.
- Confirmed `skg.gravity.selection._instrument_spheres(['BA-03','DP-01','IF-01','WB-01','HO-01'])` returns only `host` and `web`.
- Confirmed `_observed_tooling_boost('binary_analysis', ...)` returns `0.0` for `domain_hints=['binary']` when no explicit `instrument_hints` are present.
- Confirmed `domain_to_sphere('data_pipeline') == 'data_pipeline'`, and a `data_pipeline` local therefore misses topology rows keyed as `data`.

## Findings Added This Slice

1. `kernel.adapters` still relies on filename shape too early, so identity-aware loading can miss discovery artifacts for hostname aliases.
2. The gravity/kernel helper layer still has a second domain-alias seam beyond `wicket_graph` and `KernelStateEngine`: `selection.py` and `field_functional.py` normalize current binary/data/IoT domains inconsistently.
3. Current tests cover event identity and high-level gravity domain behavior, but they do not pin hostname-alias discovery loading or current-sphere inference for `BA-*` / `DP-*` / `IF-*` families.

## Artifact Updates

- Updated `review/CANONICAL_MODEL.md`
- Updated `review/REVIEW_LEDGER.md`
- Updated `review/OPEN_ISSUES.md`
