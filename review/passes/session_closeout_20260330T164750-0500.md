# Session Closeout: 2026-03-30T16:47:50-0500

## Scope

Continued the file-by-file audit through the host toolchain adapters, host projector, host golden test, and the remaining small toolchain bootstrap scripts.

Files opened in this slice:

- `skg-host-toolchain/adapters/ssh_collect/parse.py`
- `skg-host-toolchain/adapters/winrm_collect/parse.py`
- `skg-host-toolchain/adapters/nmap_scan/parse.py`
- `skg-host-toolchain/projections/host/run.py`
- `skg-host-toolchain/tests/test_golden.py`
- `skg-host-toolchain/bootstrap.sh`
- `skg-ad-lateral-toolchain/bootstrap.sh`
- `skg-aprs-toolchain/bootstrap.sh`
- `skg-container-escape-toolchain/bootstrap.sh`

## Findings

1. The host toolchain is internally coherent on its checked-in projector fixture.
   - `skg-host-toolchain/tests/test_golden.py` passed.
   - Coverage remains projector-heavy; it does not exercise `ssh_collect`, `winrm_collect`, or `nmap_scan`.

2. The nested-projector sheaf shim issue also affects the host projector.
   - `skg-host-toolchain/projections/host/run.py` uses `Path(__file__).resolve().parents[4]`, which resolves to `/opt`, not `/opt/skg`.

3. `ssh_collect` has a real hygiene defect.
   - `skg-host-toolchain/adapters/ssh_collect/parse.py` contains a second unreachable copy of the main connection and HO-* collection flow after the first `return 0`.

4. Small toolchain bootstrap scripts are inconsistent.
   - AD lateral, APRS, and container escape bootstraps run their golden tests.
   - Host bootstrap only creates the venv and installs requirements; it does not run its own golden test.

## Validation

Executed successfully:

- `python /opt/skg/skg-host-toolchain/tests/test_golden.py`

Also confirmed:

- `Path('/opt/skg/skg-host-toolchain/projections/host/run.py').resolve().parents[4] == /opt`
- repo-wide search found no direct behavioral tests for `ssh_collect`, `winrm_collect`, or `nmap_scan`

## Artifact Updates

Updated:

- `review/CANONICAL_MODEL.md`
- `review/REVIEW_LEDGER.md`
- `review/OPEN_ISSUES.md`

No fixes were applied.
