# Session Closeout: 2026-03-30T16:40:14-0500

## Scope

Continued the file-by-file repository audit through additional toolchain-root wrappers and projectors. No runtime or code fixes were applied.

Files opened in this slice:

- `skg-iot_firmware-toolchain/adapters/firmware_probe/__init__.py`
- `skg-iot_firmware-toolchain/projections/iot_firmware/run.py`
- `skg-data-toolchain/projections/data/run.py`
- `skg-aprs-toolchain/skg.py`
- `skg-aprs-toolchain/projections/aprs/run.py`
- `skg-container-escape-toolchain/skg_escape.py`
- `skg-container-escape-toolchain/projections/escape/run.py`
- `skg-host-toolchain/skg_host.py`

## New Findings

1. The IoT firmware toolchain currently contains two divergent adapter implementations under one package surface.
   - `adapters/firmware_probe/__init__.py` is an SSH/image collector with default path `firmware_rce_via_busybox_v1`.
   - `adapters/firmware_probe/probe.py` is a network/banner/HTTP probe with default path `iot_firmware_rce_v1`.

2. Several generated or template-derived projectors use a brittle optional sheaf-import shim.
   - `skg-iot_firmware-toolchain/projections/iot_firmware/run.py`
   - `skg-data-toolchain/projections/data/run.py`
   - `skg-container-escape-toolchain/projections/escape/run.py`
   - In all three cases, `Path(__file__).resolve().parents[4]` resolves to `/opt`, not `/opt/skg`.

3. APRS remains on an older but internally coherent catalog contract.
   - Both `skg-aprs-toolchain/skg.py` and `skg-aprs-toolchain/projections/aprs/run.py` default to `contracts/catalogs/attack_preconditions_catalog.v1.json`.

4. `skg-host-toolchain/skg_host.py` is comparatively aligned with current host-toolchain contracts.
   - It defaults to `attack_preconditions_catalog.host.v1.json`.
   - Its `latest` helper filters `workload_id` correctly.

## Validation

- Path-resolution validation confirmed `Path(__file__).resolve().parents[4] == /opt` for the IoT, data, and container-escape nested projectors listed above.
- No live runtime validation or code changes were performed in this slice.

## Artifact Updates

Updated:

- `review/CANONICAL_MODEL.md`
- `review/REVIEW_LEDGER.md`
- `review/OPEN_ISSUES.md`

## Next Audit Slice

Continue through remaining unread toolchain-root helpers and adapters, then proceed into any remaining support modules not yet opened so the repo approaches a true file-by-file baseline before repair planning.
