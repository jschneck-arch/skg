# Session Closeout 20260330T183821-0500

## Scope

Continued the read-first audit through the remaining kernel support layer and its direct gravity-facing tests. This slice finished `kernel.engine`, `kernel.folds`, `kernel.gravity`, `kernel.support`, `kernel.state`, `kernel.energy`, and the unread remainder of `tests/test_gravity_runtime.py`, then reconciled those findings into the review artifacts.

## Files Opened

- `skg/kernel/engine.py`
- `skg/kernel/folds.py`
- `skg/kernel/gravity.py`
- `skg/kernel/support.py`
- `skg/kernel/state.py`
- `skg/kernel/energy.py`
- `tests/test_gravity_runtime.py`
- `tests/test_sensor_projection_loop.py` (kernel naming/fiber slices)
- `review/CANONICAL_MODEL.md`
- `review/REVIEW_LEDGER.md`
- `review/OPEN_ISSUES.md`

## Narrow Validation

- Confirmed `KernelStateEngine._infer_domain_wickets(...)` still groups active `AD-01`, `BA-01`, `DP-01`, `DE-01`, and `IF-01` wickets under `unknown`.
- Confirmed `KernelStateEngine.field_locals(...)` groups those same active wickets into one `unknown` local when `domain_wickets` is omitted.
- Confirmed kernel/gravity test coverage is split across old and new naming: `test_sensor_projection_loop.py` still uses `DA-01` / `data`, while `test_gravity_runtime.py` already expects `data_pipeline` / `binary_analysis`.

## Findings Added This Slice

1. `KernelStateEngine` carries a second stale prefix-to-domain map separate from `wicket_graph`.
2. Fallback field-local grouping and fiber scoring can therefore lose active AD, binary, data, and IoT separation unless explicit `domain_wickets` are provided.
3. Current kernel/gravity coverage is not consistent enough to catch that naming drift because tests still pin both old and new domain vocabularies.
4. `state_db.py` remains better described as a write-side telemetry shadow than as a live query boundary.

## Artifact Updates

- Updated `review/CANONICAL_MODEL.md`
- Updated `review/REVIEW_LEDGER.md`
- Updated `review/OPEN_ISSUES.md`
