# Session Closeout: 2026-03-30T16:24:36-0500

## Scope

Line-by-line audit continuation across the temporal/catalog/gravity-sidecar slice:

- `skg/catalog/compiler.py`
- `skg/catalog/__init__.py`
- `skg/core/__init__.py`
- `skg/modes/__init__.py`
- `skg/temporal/__init__.py`
- `skg-gravity/gravity.py`
- `skg-gravity/gravity_web.py`
- `skg-gravity/exploit_proposals.py`
- binary fixture boundary for `tests/fixtures/webapp.db`

## Files Opened In This Pass

- `skg/catalog/compiler.py`
- `skg/catalog/__init__.py`
- `skg/core/__init__.py`
- `skg/modes/__init__.py`
- `skg/temporal/__init__.py`
- `skg-gravity/gravity.py`
- `skg-gravity/gravity_web.py`
- `skg-gravity/exploit_proposals.py`
- `tests/fixtures/webapp.db` via SQLite schema/content inspection

## Validations Run

- targeted temporal validation for `DeltaStore.calibrate_confidence_weights()`
- repo-wide naming search for compiled catalog output conventions
- repo-wide callsite search for `gravity_web.py` and `exploit_proposals.py`
- SQLite schema and sample-row inspection for `tests/fixtures/webapp.db`

## Refined Findings

### 1. Temporal confidence calibration is currently disconnected from live transition data

- `calibrate_confidence_weights()` expects `evidence_rank`
- `WicketTransition` does not store `evidence_rank` or metadata carrying it
- direct validation confirmed real transitions still yield only default `insufficient_data` outputs

Conclusion:

The calibration helper is currently more of a design stub than a working empirical calibration path.

### 2. The standalone YAML catalog compiler has drifted from the live toolchain convention

- the compiler writes `attack_preconditions_catalog.v1.{domain}.json`
- the checked-in toolchains and generator use `attack_preconditions_catalog.{domain}.v1.json`

Conclusion:

`skg.catalog.compiler` appears to be a secondary authoring path with an output contract that no longer matches the canonical runtime/toolchain naming convention.

### 3. Smaller gravity sidecars look unwired

- `gravity.py` is only a shim into `gravity_field.py`
- repo-wide callsite search found no live users of `gravity_web.py` or `exploit_proposals.py`
- `exploit_proposals.py` also writes into a separate proposal directory

Conclusion:

These modules currently look ceremonial/legacy rather than canonical runtime paths.

### 4. The `webapp.db` fixture is coherent with the data-test story

- schema and sample rows match the intentional violations described by `create_webapp_db.py`
- duplicate order ids, nullable emails, and session data are present as expected

Conclusion:

The SQLite fixture itself looks current and aligned with the data-pipeline tests.

## Recommended Immediate Review Batch

1. Remaining unread small support modules and package files outside the already-covered runtime core
2. Any unread toolchain root helpers or generated support files still not opened line by line
3. Binary/non-text artifact boundaries that affect packaging or tests but were only schema-inspected here

## No-Fix Boundary

No runtime fixes were applied. This pass only extended the audit record and added narrow validation.
