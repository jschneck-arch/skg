# Session Closeout: 2026-03-30 17:09:47 -0500

## Scope

Continued the file-by-file audit through the remaining unread web SSH collector path and the remaining auxiliary AI/binary adapters.

## Files Opened

- `skg-web-toolchain/adapters/ssh_collect/parse.py`
- `skg-web-toolchain/adapters/ssh_collect/__init__.py`
- `skg-web-toolchain/adapters/web_active/__init__.py`
- `skg-nginx-toolchain/adapters/ssh_collect/__init__.py`
- `skg-nginx-toolchain/projections/nginx/__init__.py`
- `skg-ai-toolchain/adapters/ai_probe/probe.py`
- `skg-binary-toolchain/adapters/capa_analysis/parse.py`
- `skg-binary-toolchain/adapters/frida_trace/parse.py`
- `skg-binary-toolchain/adapters/angr_symbolic/parse.py`

## Validation

- Repo-wide reference search confirmed `skg-web-toolchain/adapters/ssh_collect/parse.py` currently surfaces only in itself, `forge_staging`, and older evidence/doc artifacts, not in active tests or other obvious runtime callsites.
- Repo-wide search confirmed there are still no direct behavioral tests for `ai_probe`, `capa_analysis`, `angr_symbolic`, `frida_trace`, or the legacy web SSH collector.
- No live target collection, runtime fixes, or networked scans were performed.

## Findings

- `skg-web-toolchain/adapters/ssh_collect/parse.py` is a stale legacy web path inside the canonical web tree: it emits `W-01..W-05`, not `WB-*`, and the checks are Apache/APR-specific rather than aligned with the checked-in current web catalog.
- The AI probe is comparatively contract-aligned: it uses the normal event envelope and a programmatic `run()`-style probe flow, but it still lacks direct behavioral coverage.
- The auxiliary binary adapters are also structurally cleaner than many other generated roots:
  - `capa_analysis` emits `BA-07` / `BA-08` with the normal envelope and a programmatic `run()` entry point.
  - `angr_symbolic` emits `BA-09` with the normal envelope and a programmatic `run()` entry point.
  - `frida_trace` emits `BA-10` with the normal envelope and an explicit `authorized=True` gate plus a programmatic `run()` entry point.
- Despite that cleaner structure, those AI/binary auxiliary adapters still have little or no direct behavioral coverage in-tree.

## Artifact Updates

- Updated `review/CANONICAL_MODEL.md`
- Updated `review/REVIEW_LEDGER.md`
- Updated `review/OPEN_ISSUES.md`
