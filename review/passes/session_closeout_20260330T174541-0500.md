# Session Closeout 20260330T174541-0500

## Scope

Continued the read-first gravity/support audit. This slice covered the previously unread early and mid sections of `skg-gravity/gravity_field.py`, the full native BloodHound sensor path, the remainder of `skg/sensors/adapter_runner.py`, and the remaining `sysaudit` adapter body.

## Files Opened

- `skg-gravity/gravity_field.py` lines `1-2335`
- `skg/sensors/bloodhound_sensor.py`
- `skg/sensors/adapter_runner.py`
- `skg-host-toolchain/adapters/sysaudit/audit.py`
- `review/CANONICAL_MODEL.md`
- `review/REVIEW_LEDGER.md`
- `review/OPEN_ISSUES.md`

## Narrow Validation

- Confirmed `_merge_configured_targets(...)` raises `AttributeError` on a list-root `targets.yaml`, even though other repo paths support that inventory shape.
- Confirmed `detect_instruments()` marks `bloodhound` unavailable when the CE URL is unreachable even if `NEO4J_PASSWORD` is configured.

## Findings Added This Slice

1. Gravity target merge still assumes dict-shaped `targets.yaml`; list-root configs can crash the live gravity loop.
2. BloodHound availability logic does not honor its own Neo4j fallback path.
3. BloodHound sensor domain scoping is weaker than advertised: `domain_sid` is accepted but not enforced in collection, and default workload identity falls back to the collector endpoint hostname.
4. `execute_instrument()` still contains duplicated dead dispatch branches for `iot_firmware`, `ai_probe`, and `supply_chain`.
5. `sysaudit/audit.py` is a real, substantial integrity adapter; no new major contract breaks were found beyond the already-recorded gravity-shell omission of projection.

## Artifact Updates

- Updated `review/CANONICAL_MODEL.md`
- Updated `review/REVIEW_LEDGER.md`
- Updated `review/OPEN_ISSUES.md`
