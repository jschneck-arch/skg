# Session Closeout 20260330T202305-0500

## Scope

Continued the read-first audit through daemon projection lookup and topology world-state / field-pull helpers, then checked the direct regression slices that still cover those paths.

## Files Opened

- `skg/core/daemon.py`
- `skg/core/daemon_registry.py`
- `skg/topology/energy.py`
- `tests/test_runtime_regressions.py`
- `tests/test_sensor_projection_loop.py`
- `review/CANONICAL_MODEL.md`
- `review/REVIEW_LEDGER.md`
- `review/OPEN_ISSUES.md`

## Narrow Validation

- Confirmed `daemon._find_projection_files(...)` returns a `domain='data'` projection when queried as `data`, but returns `[]` for that same artifact when queried as `data_pipeline`.
- Confirmed `anchored_field_pull(..., domains={'binary_analysis'}, sphere_pulls={'binary': 10.0}, ...) == 0.0`, while equivalent current-name cases for `data_pipeline`, `container_escape`, and `ad_lateral` still produce nonzero pull.
- Confirmed `_world_states_from_surface(...)` given target domains `['data_pipeline','binary_analysis','container_escape','ad_lateral']` returns only `ad` and `container`.

## Findings Added This Slice

1. Daemon projection lookup still carries only a binary alias bridge, so daemon projection endpoints can miss legacy `data` artifacts when queried with current `data_pipeline` naming.
2. Topology helper mappings still treat current `binary_analysis` differently from the `binary` sphere and still drop `data_pipeline` / `binary_analysis` when lifting discovery surfaces into supplementary world-state.
3. Existing regression coverage still pins mostly legacy `data` / `container` names in the relevant topology slices and only covers binary aliasing on the daemon side.

## Artifact Updates

- Updated `review/CANONICAL_MODEL.md`
- Updated `review/REVIEW_LEDGER.md`
- Updated `review/OPEN_ISSUES.md`
