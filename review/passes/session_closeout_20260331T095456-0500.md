# Session Closeout — 2026-03-31T09:54:56-05:00

Scope for this pass:

- Continue line-by-line daemon/sensor/web/CLI shell audit with no runtime code fixes.
- Re-validate remaining `/collect` and run-scoped projection assumptions.
- Keep review artifacts (`OPEN_ISSUES`, `CANONICAL_MODEL`, `REVIEW_LEDGER`) aligned to current code.

What was validated:

- `collect_host()` now returns `False` on zero-event runs and `True` only when events are emitted.
- Injected-target SSH collection now uses injected targets and bypasses interval gating.
- Daemon `/collect` now defaults single-target workload ids to `ssh::{target}`.
- Sweep run-scoping is still broken for auto-projection:
  - Sensor loop filters with `*_{sweep_run_id}.ndjson`.
  - Active sensors emit files with independent run-id suffixes (SSH per-target UUID) or no run-id suffix (web sensor).
  - Result: no sweep-id filename matches in targeted validation.
- Daemon web unknown-node hint mapping is stale:
  - `_resolution_hint('WB-01')` and `_resolution_hint('WEB-01')` return generic fallback text.
  - `_compute_field_state_inner()` carries that generic fallback into `resolution_required` for unknown web wickets.

Review artifact updates:

- `review/CANONICAL_MODEL.md`
  - Updated run-scoped projection drift wording to reflect sweep-id mismatch against producer run-id behavior (not “no run-id support”).
- `review/REVIEW_LEDGER.md`
  - Corrected `/collect` and sensor-loop summary statements.
  - Corrected stale collect/runtime-test bullets.
  - Added explicit validation note for stale web resolution hints.
- `review/OPEN_ISSUES.md`
  - Rewrote `HIGH-01` as downgraded/historical (largely fixed in current tree).
  - Updated `MED-07` evidence to current run-id mechanics.
  - Added `MED-79` for stale `_resolution_hint(...)` web prefix handling.

Code fixes applied:

- None (analysis-only pass).
