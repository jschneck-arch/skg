# skg-domain-host

Second-domain canonical migration for the host domain.

This pack contains host ontology, mappings, adapters, projectors, and explicit
policy artifacts under `packages/skg-domains/host/src/skg_domain_host`.

Manifest authority: `src/skg_domain_host/manifest.yaml`.
