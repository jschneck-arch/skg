# Phase 4C Deletion Ledger

Date: 2026-04-01  
Scope: controlled fallback deletion, Waves 1–2 only.

## Deleted Fallback Branches

| Legacy file | Deleted symbol/branch/import path | Canonical replacement | Why deletion was safe | Validation performed | Residual risk |
|---|---|---|---|---|---|
| `skg/cli/commands/derived.py` | `_rebuild_interp_from_events()` fallback import: `from skg.sensors.projector import project_events_dir` | `from skg_services.gravity.projector_runtime import project_events_dir` | Wave 1 runtime callsite was already migrated in 4B and canonical projector runtime is active. | `py_compile` on file; package pytest; bootstrap enforcement test. | Fails fast if canonical packages are missing from runtime path. |
| `skg/cli/commands/exploit.py` | `_project_binary_events()` fallback import: `from skg.sensors.projector import project_events` | `from skg_services.gravity.projector_runtime import project_events` | Binary projection path already used canonical runtime in 4B with compatibility fallback only. | `py_compile` on file; package pytest; CLI import smoke in validation script. | Import-time failure if canonical package path is absent. |
| `skg/core/daemon.py` | `/collect` fallback import: `from skg.sensors.projector import project_events_dir` | `from skg_services.gravity.projector_runtime import project_events_dir` | Daemon collection/projection route already canonicalized; fallback no longer needed. | `py_compile` on file; daemon import smoke (`DOMAINS`/inventory load). | Collect endpoint now hard-requires canonical service package availability. |
| `skg/forge/generator.py` | projector registration fallback import: `from skg.sensors.projector import _discover_toolchain_projector, _projector_cache` | `from skg_services.gravity.projector_runtime import _discover_toolchain_projector, _projector_cache` | Install-time projector activation now aligns with service runtime ownership. | `py_compile` on file; package pytest. | Still relies on private runtime symbols; public API still pending. |
| `skg/sensors/__init__.py` | `SensorLoop._auto_project_all()` fallback import: `from skg.sensors.projector import project_event_file, project_events_dir` | `from skg_services.gravity.projector_runtime import project_event_file, project_events_dir` | Sensor loop callsite already migrated; this makes canonical projector runtime mandatory. | `py_compile` on file; package pytest. | Sensor loop now hard-fails if canonical projector runtime import fails. |
| `skg/core/daemon.py` | top-level fallback import block for registry/runtime composition (`try ... except` setting `_CanonicalDomainRegistry=None`, `_service_* = None`) | direct imports of `skg_registry.DomainRegistry` and `skg_services.gravity.domain_runtime.load_daemon_domains_from_inventory` | Wave 2 requires canonical registry/service runtime composition; legacy branch is superseded. | `py_compile` on file; daemon import smoke; package pytest. | Daemon startup now hard-fails if canonical packages are unavailable. |
| `skg/core/daemon.py` | `_load_domains_and_inventory()` legacy fallback branch importing `skg.core.domain_registry` | canonical-only composition via `_canonical_inventory_rows()` + `load_daemon_domains_from_inventory()` | Runtime callsites already migrated to canonical composition in 4B. | daemon import smoke; package pytest; bootstrap enforcement test. | Domain row-shape assumptions still depend on canonical metadata quality. |
| `skg/core/coupling.py` | fallback import block for `_CanonicalDomainRegistry` and `_known_domain_names()` fallback to `skg.core.domain_registry.load_domain_inventory` | direct `DomainRegistry` import and canonical domain-name discovery | Coupling validation only requires domain names; canonical registry is authoritative source. | `py_compile` on file; package pytest; bootstrap enforcement test. | Validation now assumes canonical registry discovery succeeds in all supported environments. |
| `skg/sensors/dark_hypothesis_sensor.py` | fallback import block for `_CanonicalDomainRegistry` and fallback to `load_domain_inventory()` in `_available_instruments()` | direct canonical registry discovery (`DomainRegistry.discover().list_domains()`) | Instrument discovery is now registry-owned; fallback path no longer needed for canonical runtime. | `py_compile` on file; package pytest; bootstrap enforcement test. | Sensor now depends on canonical registry package availability at runtime. |
| `skg/sensors/projector.py` | fallback import block for `_CanonicalDomainRegistry` and `_domain_inventory_rows()` fallback to `load_domain_inventory()` | direct canonical registry discovery in `_domain_inventory_rows()` | Registry fallback branch was superseded by canonical discovery in 4B. | `py_compile` on file; package pytest; bootstrap enforcement test. | Legacy projector shim still exists for other fallback concerns (service runtime fallback). |

## Targeted Test Updates

- Added canonical bootstrap enforcement test: [test_phase4c_bootstrap_enforcement.py](/opt/skg/packages/skg-services/tests/test_phase4c_bootstrap_enforcement.py)
- Added assertions that Wave 1/2 legacy fallback imports are absent and canonical imports are present.
